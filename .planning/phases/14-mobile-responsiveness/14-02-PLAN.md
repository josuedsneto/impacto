---
phase: 14-mobile-responsiveness
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/app/app/dashboard/page.tsx
  - frontend/components/dashboard/ToolGrid.tsx
autonomous: true
requirements:
  - MOB-03

must_haves:
  truths:
    - "The Prices section (PriceCard x2) shows as a single column at 375px"
    - "The Live Widgets section (NewsFeed + FocusWidget + AccountSummary) stacks to 1 col at 375px"
    - "The ToolGrid shows 2 columns at 375px, not 5 columns"
    - "No inline gridTemplateColumns style remains on layout-critical grid divs in dashboard"
  artifacts:
    - path: "frontend/app/app/dashboard/page.tsx"
      provides: "Dashboard with responsive grid classes replacing inline gridTemplateColumns styles"
    - path: "frontend/components/dashboard/ToolGrid.tsx"
      provides: "ToolGrid with responsive grid classes replacing repeat(5,1fr) inline style"
  key_links:
    - from: "frontend/app/app/dashboard/page.tsx"
      to: "frontend/components/dashboard/ToolGrid.tsx"
      via: "import { ToolGrid }"
      pattern: "ToolGrid"
---

<objective>
Fix the two inline `gridTemplateColumns` style overrides on `dashboard/page.tsx` and the `repeat(5, 1fr)` grid on `ToolGrid.tsx`. Inline styles have higher CSS specificity than Tailwind classes, so they must be removed and replaced with Tailwind responsive grid classes.

Purpose: The dashboard currently renders a 2-column price grid and a 5-column tool grid at all viewport sizes, including 375px mobile where both overflow. This plan makes those grids stack to 1-2 columns on mobile.

Output: `dashboard/page.tsx` and `ToolGrid.tsx` with all inline `gridTemplateColumns` replaced by Tailwind responsive `grid-cols-*` classes.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-mobile-responsiveness/14-RESEARCH.md
@frontend/app/app/dashboard/page.tsx
@frontend/components/dashboard/ToolGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard/page.tsx inline grid styles</name>
  <files>
    frontend/app/app/dashboard/page.tsx
  </files>
  <action>
    In `frontend/app/app/dashboard/page.tsx`, make these two targeted changes:

    **Change 1 — Prices section (around line 232):**
    Find:
    ```tsx
    <div className="grid gap-4" style={{ gridTemplateColumns: "1fr 1fr" }}>
    ```
    Replace with:
    ```tsx
    <div className="grid gap-4 grid-cols-1 md:grid-cols-2">
    ```
    Remove the `style={{ gridTemplateColumns: "1fr 1fr" }}` entirely — leaving it alongside the Tailwind classes would cause the inline style to override Tailwind at all breakpoints.

    **Change 2 — Live Widgets section (around line 258):**
    Find:
    ```tsx
    <div className="grid gap-4" style={{ gridTemplateColumns: "1fr 1fr 1fr" }}>
    ```
    Replace with:
    ```tsx
    <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3">
    ```
    Remove the `style={{ gridTemplateColumns: "1fr 1fr 1fr" }}` entirely.

    No other changes to this file. Do not touch the ticker tape, the header section, imports, or server components.
  </action>
  <verify>
    <automated>cd C:/Users/netin/OneDrive/Documentos/Code/impacto/frontend && grep -n "gridTemplateColumns" app/app/dashboard/page.tsx | wc -l</automated>
    <manual>The command should output 0 — no remaining inline gridTemplateColumns in dashboard/page.tsx</manual>
  </verify>
  <done>
    Zero occurrences of `gridTemplateColumns` in `dashboard/page.tsx`.
    The two grid divs now use `grid-cols-1 md:grid-cols-2` and `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` respectively.
    TypeScript compilation still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ToolGrid.tsx inline grid style</name>
  <files>
    frontend/components/dashboard/ToolGrid.tsx
  </files>
  <action>
    In `frontend/components/dashboard/ToolGrid.tsx`, find the single grid container:

    Find (around line 65):
    ```tsx
    <div className="grid gap-3" style={{ gridTemplateColumns: "repeat(5, 1fr)" }}>
    ```
    Replace with:
    ```tsx
    <div className="grid gap-3 grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">
    ```
    Remove the `style={{ gridTemplateColumns: "repeat(5, 1fr)" }}` entirely.

    No other changes to this file. The card hover handlers (`onMouseEnter`, `onMouseLeave`) stay as-is — they control visual properties (borderColor, background) not layout, so inline styles are acceptable there.
  </action>
  <verify>
    <automated>cd C:/Users/netin/OneDrive/Documentos/Code/impacto/frontend && grep "gridTemplateColumns" components/dashboard/ToolGrid.tsx | wc -l</automated>
    <manual>The command should output 0 — no remaining inline gridTemplateColumns in ToolGrid.tsx</manual>
  </verify>
  <done>
    Zero occurrences of `gridTemplateColumns` in `ToolGrid.tsx`.
    The grid div now uses `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5`.
    TypeScript compilation passes: `cd frontend && npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "gridTemplateColumns" frontend/app/app/dashboard/page.tsx frontend/components/dashboard/ToolGrid.tsx` — must return no output
2. `grep "grid-cols-1" frontend/app/app/dashboard/page.tsx` — must match (confirms mobile-first class added)
3. `grep "grid-cols-2" frontend/components/dashboard/ToolGrid.tsx` — must match
4. `cd frontend && npx tsc --noEmit` — must exit 0
</verification>

<success_criteria>
- `dashboard/page.tsx` has no inline `gridTemplateColumns` styles on layout-critical grids
- `ToolGrid.tsx` has no inline `gridTemplateColumns` style
- Both files use Tailwind responsive `grid-cols-*` classes
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-responsiveness/14-02-SUMMARY.md` following the summary template.
</output>
