---
phase: 09-fix-mkt03-param01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - frontend/components/market/TickerSuggestForm.tsx
autonomous: true
requirements:
  - MKT-03
gap_closure: true

must_haves:
  truths:
    - "POST /api/market/suggest inserts a row in tickers_catalog without DB error"
    - "adicionado_por column is populated with the authenticated user's id"
    - "Any tipo value submitted from the frontend is accepted by the DB CHECK constraint"
  artifacts:
    - path: "backend/main.py"
      provides: "Fixed TickerSuggestRequest model and insert payload"
      contains: "adicionado_por"
    - path: "frontend/components/market/TickerSuggestForm.tsx"
      provides: "Dropdown options matching DB CHECK constraint values"
      contains: "commodity"
  key_links:
    - from: "backend/main.py TickerSuggestRequest.tipo"
      to: "DB CHECK (tipo IN ('commodity','fx','acao','indice'))"
      via: "Pydantic model default + Literal enum"
      pattern: "Literal\\[.commodity"
    - from: "backend/main.py insert payload"
      to: "tickers_catalog.adicionado_por"
      via: "dict key name"
      pattern: "adicionado_por.*user\\[.id"
---

<objective>
Fix two runtime bugs that cause every POST /api/market/suggest to fail with a Supabase error:
1. Insert payload uses key `suggested_by` but the column is `adicionado_por`.
2. `TickerSuggestRequest.tipo` defaults to `"equity"` which is not in the DB CHECK constraint; frontend dropdown options are also misaligned.

Purpose: Unblock the ticker suggestion flow (MKT-03) so users can submit new tickers for admin review.
Output: Fixed backend model + insert dict; updated frontend Select options matching DB values.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
@frontend/components/market/TickerSuggestForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TickerSuggestRequest model and insert payload column name</name>
  <files>backend/main.py</files>
  <action>
    Two edits in backend/main.py:

    1. TickerSuggestRequest model (around line 57-60):
       Change:
         tipo: str = "equity"  # equity | futures | fx | etf | crypto
       To:
         tipo: Literal["commodity", "fx", "acao", "indice"] = "commodity"
       Add `from typing import Literal` to imports if not already present (check top of file — `Annotated` is already imported from typing so just add `Literal` to that import).

    2. Insert payload in suggest_ticker() (around line 126-133):
       Change:
         "suggested_by": user["id"],
       To:
         "adicionado_por": user["id"],

    Do not change any other fields in the insert dict or any other part of the function.
  </action>
  <verify>
    <automated>cd C:/Users/netin/OneDrive/Documentos/Code/impacto/backend && python -c "from main import TickerSuggestRequest; r = TickerSuggestRequest(ticker='TEST'); assert r.tipo == 'commodity', f'got {r.tipo}'; import ast, pathlib; src = pathlib.Path('main.py').read_text(); assert 'adicionado_por' in src and 'suggested_by' not in src.split('adicionado_por')[0].split('suggest_ticker')[1], 'column name not fixed'; print('OK')"</automated>
  </verify>
  <done>TickerSuggestRequest.tipo defaults to "commodity" and only accepts DB-valid values; insert payload uses key "adicionado_por".</done>
</task>

<task type="auto">
  <name>Task 2: Align frontend TickerSuggestForm tipo dropdown with DB values</name>
  <files>frontend/components/market/TickerSuggestForm.tsx</files>
  <action>
    In frontend/components/market/TickerSuggestForm.tsx:

    1. Change initial state:
       From: const [tipo, setTipo] = useState("equity");
       To:   const [tipo, setTipo] = useState("commodity");

    2. Replace the SelectContent children (SelectItem elements) with options that exactly match DB CHECK values:
       - value="commodity" → label "Commodity / Futuro"
       - value="fx"        → label "Câmbio (FX)"
       - value="acao"      → label "Ação"
       - value="indice"    → label "Índice"

    Remove the old equity, futures, etf, crypto options entirely. Keep all surrounding JSX structure unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/netin/OneDrive/Documentos/Code/impacto/frontend && node -e "const src = require('fs').readFileSync('components/market/TickerSuggestForm.tsx','utf8'); const valid = ['commodity','fx','acao','indice']; const invalid = ['equity','futures','etf','crypto']; invalid.forEach(v => { if(src.includes('value=\"'+v+'\"')) throw new Error('Still has invalid value: '+v); }); valid.forEach(v => { if(!src.includes('value=\"'+v+'\"')) throw new Error('Missing value: '+v); }); console.log('OK');"</automated>
  </verify>
  <done>Frontend dropdown shows exactly 4 options (commodity, fx, acao, indice) matching DB CHECK constraint. Default is "commodity".</done>
</task>

</tasks>

<verification>
After both tasks:
- `python -c "from main import TickerSuggestRequest; r = TickerSuggestRequest(ticker='X'); assert r.tipo == 'commodity'"` passes in backend/
- No occurrence of `"suggested_by"` in the suggest_ticker insert payload
- No occurrence of `value="equity"` or `value="futures"` or `value="etf"` or `value="crypto"` in TickerSuggestForm.tsx
</verification>

<success_criteria>
- TickerSuggestRequest.tipo is a Literal with 4 DB-valid values, default "commodity"
- Insert payload key is "adicionado_por" not "suggested_by"
- Frontend dropdown options exactly match ('commodity','fx','acao','indice')
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-mkt03-param01/09-01-SUMMARY.md`
</output>
