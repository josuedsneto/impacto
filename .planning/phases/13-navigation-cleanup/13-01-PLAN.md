---
phase: 13-navigation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/app/layout.tsx
autonomous: false
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Sidebar não exibe Metas, Jump Diffusion, Payoff Opções, Risco, Cenários, Relatório Focus, ARIMA Açúcar, ARIMA Dólar e Opções"
    - "URLs das páginas ocultas continuam acessíveis diretamente (ex: /app/metas, /app/arima, /app/options)"
    - "Nenhum arquivo de rota foi deletado — apenas removido do componente de navegação"
  artifacts:
    - path: "frontend/app/app/layout.tsx"
      provides: "Sidebar navigation with 9 pages excluded from NAV_SECTIONS"
      contains: "NAV_SECTIONS"
    - path: "frontend/app/app/metas/page.tsx"
      provides: "Metas route still exists (accessible via URL)"
    - path: "frontend/app/app/arima/page.tsx"
      provides: "ARIMA route still exists (accessible via URL)"
    - path: "frontend/app/app/options/page.tsx"
      provides: "Options route still exists (accessible via URL)"
  key_links:
    - from: "frontend/app/app/layout.tsx"
      to: "NAV_SECTIONS array"
      via: "commented-out items"
      pattern: "//.*href.*app/(metas|jump-diffusion|options|risco|cenarios|focus|arima)"
---

<objective>
Verify and finalize the navigation cleanup for Phase 13: confirm that the 9 pages required by NAV-01 are absent from the sidebar while their routes remain intact per NAV-02.

Purpose: The layout.tsx was partially updated during Phase 12 with commented-out nav items. This plan audits the current state, makes any remaining adjustments, and provides human verification that the sidebar looks correct.

Output: Confirmed layout.tsx where NAV_SECTIONS excludes the 9 pages, all route directories intact, human visual sign-off.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/app/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and finalize nav exclusions in layout.tsx</name>
  <files>frontend/app/app/layout.tsx</files>
  <action>
    Read frontend/app/app/layout.tsx and audit the NAV_SECTIONS array against the 9 pages that must be hidden per NAV-01:
    - Metas (/app/metas)
    - Jump Diffusion (/app/jump-diffusion)
    - Payoff Opções (/app/options)
    - Risco (/app/risco)
    - Cenários (/app/cenarios)
    - Relatório Focus (/app/focus)
    - ARIMA Açúcar — mapped to /app/arima (single ARIMA page covers both ARIMA variants)
    - ARIMA Dólar — same /app/arima page
    - Opções (/app/options — same route as Payoff Opções, or check if there is a separate pricer page)

    For each of the 9 items: if its href still appears as an uncommented entry in NAV_SECTIONS, remove it. Do NOT delete the href line entirely — comment it out with `// ` prefix so the intent is preserved (matches the existing pattern in the file).

    Also verify: do NOT remove Dashboard, Monte Carlo (/app/simulation), Volatilidade, VaR, Breakeven, Stress Test, or Notícias — these must remain visible.

    If the file already satisfies all requirements with no changes needed, do not modify it (write it only if changes are required).

    After the audit, verify route directories still exist for all hidden pages by checking:
    - frontend/app/app/metas/page.tsx
    - frontend/app/app/jump-diffusion/page.tsx
    - frontend/app/app/options/page.tsx
    - frontend/app/app/risco/page.tsx
    - frontend/app/app/cenarios/page.tsx
    - frontend/app/app/focus/page.tsx
    - frontend/app/app/arima/page.tsx

    If any route directory is missing, do NOT create it — log it as a finding in the summary. Missing routes mean NAV-02 cannot be fully satisfied for that entry.
  </action>
  <verify>
    <automated>grep -E "href.*app/(metas|jump-diffusion|risco|cenarios|focus|arima)" frontend/app/app/layout.tsx | grep -v "^[[:space:]]*//" && echo "FAIL: uncommented hidden page found" || echo "PASS: all 9 pages absent from active nav"</automated>
    <manual>Confirm the above command outputs PASS. Also run: ls frontend/app/app/{metas,jump-diffusion,options,risco,cenarios,focus,arima}/page.tsx to confirm route files exist.</manual>
  </verify>
  <done>NAV_SECTIONS in layout.tsx contains no uncommented entries for the 9 hidden pages, and all 7 route directories confirmed present on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Navigation sidebar with 9 pages removed. Claude audited layout.tsx, confirmed all 9 nav items are absent from NAV_SECTIONS (commented out or never present), and verified route directories still exist on disk.
  </what-built>
  <how-to-verify>
    1. Run the dev server: cd frontend/app && npm run dev
    2. Open http://localhost:3000/app/dashboard in the browser
    3. Confirm the sidebar does NOT show any of: Metas, Jump Diffusion, Payoff Opções, Risco, Cenários, Relatório Focus, ARIMA Açúcar, ARIMA Dólar, Opções
    4. Confirm the sidebar DOES show: Dashboard, Monte Carlo, Volatilidade, VaR, Breakeven, Stress Test, Notícias
    5. Navigate directly to http://localhost:3000/app/metas — confirm the page loads (not 404)
    6. Navigate directly to http://localhost:3000/app/arima — confirm the page loads (not 404)
    7. Navigate directly to http://localhost:3000/app/cenarios — confirm the page loads (not 404)
  </how-to-verify>
  <resume-signal>Type "approved" if sidebar is clean and direct URLs work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
NAV-01 satisfied: sidebar renders only the pages NOT in the 9-page exclusion list.
NAV-02 satisfied: all route page.tsx files exist — direct URL access still works.
No route was deleted — only nav entries removed/commented in layout.tsx.
</verification>

<success_criteria>
1. Sidebar does not display: Metas, Jump Diffusion, Payoff Opções, Risco, Cenários, Relatório Focus, ARIMA Açúcar, ARIMA Dólar, Opções
2. Direct URL access to all hidden pages returns the page content (not 404)
3. layout.tsx diff shows only comment changes — no file deletions, no route deletions
</success_criteria>

<output>
After completion, create `.planning/phases/13-navigation-cleanup/13-01-SUMMARY.md` with:
- What was audited and what (if anything) was changed in layout.tsx
- List of route directories confirmed present
- Any missing route directories found (NAV-02 gaps)
- Human verification result
</output>
