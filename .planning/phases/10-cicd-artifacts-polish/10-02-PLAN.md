---
phase: 10-cicd-artifacts-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-cicd/08-01-PLAN.md
  - .planning/phases/08-cicd/08-01-SUMMARY.md
  - .planning/phases/08-cicd/08-01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INFRA-03, INFRA-04]
gap_closure: true

must_haves:
  truths:
    - ".planning/phases/08-cicd/ exists with PLAN.md, SUMMARY.md, and VERIFICATION.md"
    - "REQUIREMENTS.md marks INFRA-03 and INFRA-04 as [x] satisfied"
  artifacts:
    - path: ".planning/phases/08-cicd/08-01-PLAN.md"
      provides: "Retroactive plan documenting what was built in commit 5d1d103"
    - path: ".planning/phases/08-cicd/08-01-SUMMARY.md"
      provides: "Summary of what deploy.yml and ThemeProvider implement"
    - path: ".planning/phases/08-cicd/08-01-VERIFICATION.md"
      provides: "Verification status for INFRA-03 and INFRA-04"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "INFRA-03, INFRA-04"
      via: "status update from [ ] to [x]"
      pattern: "\\[x\\] INFRA-0[34]"
---

<objective>
Phase 8 (CI/CD & Polish) was executed directly as commit 5d1d103 without going through the GSD planning workflow. This plan creates the retroactive GSD artifacts to close the audit gap.

What was built in commit 5d1d103:
- `.github/workflows/deploy.yml` — GitHub Actions workflow using appleboy/ssh-action@v1.0.3, triggers on push to main, SSH-deploys to Oracle Cloud VM (git pull, npm ci + build, pip install, pm2 restart)
- `frontend/components/ThemeProvider.tsx` — ThemeProvider with localStorage persistence (FOUC partially fixed, blocking-script fix is in phase 10-01)

Purpose: Close the GSD artifact gap so REQUIREMENTS.md accurately reflects implementation status.
Output: 08-cicd/ directory with PLAN, SUMMARY, and VERIFICATION files; REQUIREMENTS.md updated.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create retroactive Phase 8 GSD artifacts</name>
  <files>
    .planning/phases/08-cicd/08-01-PLAN.md,
    .planning/phases/08-cicd/08-01-SUMMARY.md,
    .planning/phases/08-cicd/08-01-VERIFICATION.md
  </files>
  <action>
Create the directory `.planning/phases/08-cicd/` and write three files documenting what commit 5d1d103 built.

**08-01-PLAN.md** — retroactive plan (document intent, not executable):
```markdown
---
phase: 08-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - frontend/components/ThemeProvider.tsx
autonomous: true
requirements: [INFRA-03, INFRA-04]
retroactive: true
built_in_commit: 5d1d103
---

# Phase 8: CI/CD & Polish

## Objective
Auto-deploy on every merge to main; persist user's dark/light theme across browser sessions.

## What was built
- `.github/workflows/deploy.yml`: GitHub Actions SSH deploy on push to main using appleboy/ssh-action@v1.0.3. Requires secrets: VM_HOST, VM_USER, VM_SSH_KEY. Deploy steps: git pull, npm ci + build, pip install, pm2 restart.
- `frontend/components/ThemeProvider.tsx`: React context provider; reads/writes localStorage('theme'). Initializes as "dark" then syncs in useEffect (FOUC cosmetic issue, addressed in Phase 10).
```

**08-01-SUMMARY.md** — what was implemented:
```markdown
---
phase: 08-cicd
plan: 01
requirements: [INFRA-03, INFRA-04]
status: complete
built_in_commit: 5d1d103
---

# Phase 08 Plan 01 Summary

## What was built

### INFRA-03: Auto-deploy on push to main
- `.github/workflows/deploy.yml` created using appleboy/ssh-action@v1.0.3
- Triggers on push to `main` branch
- Deploy script: cd /opt/impacto, git pull origin main, npm ci + npm run build (frontend), pip install (backend), pm2 restart ecosystem.config.js --update-env
- Requires three GitHub repository secrets: VM_HOST, VM_USER, VM_SSH_KEY
- Secrets not yet confirmed set in GitHub (human checkpoint in Phase 10)

### INFRA-04: Theme persistence
- `frontend/components/ThemeProvider.tsx` reads/writes localStorage('theme')
- Theme persists across browser sessions
- Known cosmetic gap: FOUC on first paint (hardcoded "dark" initial state, corrected by blocking script in Phase 10-01)

## Decisions
- Used appleboy/ssh-action@v1.0.3 (stable, widely used for SSH deploys)
- PM2 restart with --update-env to pick up any new env vars
- Theme stored in localStorage (no server-side cookie needed for dark/light preference)
```

**08-01-VERIFICATION.md** — verification status:
```markdown
---
phase: 08-cicd
plan: 01
verified: false
verification_type: human_needed
requirements: [INFRA-03, INFRA-04]
---

# Phase 08 Verification

## INFRA-03: Auto-deploy
- [ ] VM_HOST secret set in GitHub repository settings
- [ ] VM_USER secret set in GitHub repository settings
- [ ] VM_SSH_KEY secret set in GitHub repository settings
- [ ] Test deploy: merge a trivial commit to main; confirm GitHub Actions workflow passes
- [ ] Confirm pm2 ls shows services running after deploy

Human verification checkpoint is in Phase 10-03.

## INFRA-04: Theme persistence
- [x] ThemeProvider reads/writes localStorage — persistence works
- [ ] FOUC: Flash of wrong theme on first paint — addressed by Phase 10-01 blocking script
- [ ] Visual verification: set light mode, hard reload, confirm no dark flash

## Notes
Phase 8 was executed as commit 5d1d103 without GSD planning artifacts. These files are retroactive documentation.
```
  </action>
  <verify>
    <automated>ls C:/Users/netin/OneDrive/Documentos/Code/impacto/.planning/phases/08-cicd/ && echo "files exist"</automated>
  </verify>
  <done>Three files exist in .planning/phases/08-cicd/ — 08-01-PLAN.md, 08-01-SUMMARY.md, 08-01-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark INFRA-03 and INFRA-04 as satisfied</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` in full. Find the entries for INFRA-03 and INFRA-04.

For each, change the status from `[ ]` (pending) to `[x]` (complete) and update any status field to reflect the current state. Add a note that secrets require human confirmation (Phase 10-03) and that the FOUC fix is in Phase 10-01.

The exact update depends on the file's current format — read it first and make surgical edits to only the INFRA-03 and INFRA-04 entries.
  </action>
  <verify>
    <automated>cd C:/Users/netin/OneDrive/Documentos/Code/impacto && grep -A2 "INFRA-03\|INFRA-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md INFRA-03 and INFRA-04 entries reflect implemented status with notes about pending human verification.</done>
</task>

</tasks>

<verification>
ls .planning/phases/08-cicd/
grep "INFRA-03\|INFRA-04" .planning/REQUIREMENTS.md
</verification>

<success_criteria>
- .planning/phases/08-cicd/ has 08-01-PLAN.md, 08-01-SUMMARY.md, 08-01-VERIFICATION.md
- REQUIREMENTS.md INFRA-03 and INFRA-04 are updated to reflect implementation status
</success_criteria>

<output>
After completion, create `.planning/phases/10-cicd-artifacts-polish/10-02-SUMMARY.md`
</output>
