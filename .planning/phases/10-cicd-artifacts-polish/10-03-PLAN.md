---
phase: 10-cicd-artifacts-polish
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified: []
autonomous: false
requirements: [INFRA-03, INFRA-04, ADM-01]
gap_closure: true

must_haves:
  truths:
    - "GitHub repo has VM_HOST, VM_USER, VM_SSH_KEY secrets confirmed and a test deploy succeeds"
    - "Non-admin authenticated users are redirected away from /app/admin"
    - "App loads in user's saved theme with no visible flash"
  artifacts:
    - path: ".planning/phases/10-cicd-artifacts-polish/10-03-SUMMARY.md"
      provides: "Human verification sign-off for all three gaps"
  key_links:
    - from: "GitHub Actions"
      to: "Oracle Cloud VM"
      via: "appleboy/ssh-action using VM_HOST, VM_USER, VM_SSH_KEY secrets"
      pattern: "deploy.yml"
---

<objective>
Human verification checkpoint for all three gap-closure items completed in plans 10-01 and 10-02.

Purpose: Confirm secrets are set in GitHub, test deploy runs successfully, verify the FOUC fix visually, and confirm admin redirect works in the running app.
Output: Confirmed end-state for INFRA-03, INFRA-04, ADM-01.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Set GitHub secrets and trigger test deploy</name>
  <what-built>deploy.yml is committed and waits for VM_HOST, VM_USER, VM_SSH_KEY secrets in GitHub repository settings.</what-built>
  <how-to-verify>
1. Go to your GitHub repository → Settings → Secrets and variables → Actions
2. Confirm (or add) three repository secrets:
   - `VM_HOST` — the public IP or domain of your Oracle Cloud VM
   - `VM_USER` — the SSH username (e.g. `ubuntu` or `opc`)
   - `VM_SSH_KEY` — the private SSH key contents (the full PEM block including headers)
3. Trigger a deploy: merge any trivial change to `main` (or manually trigger the workflow via Actions → Deploy to Production → Run workflow if you add `workflow_dispatch` to deploy.yml — or push a whitespace commit)
4. Watch the Actions run: it must show green on "SSH deploy to Oracle Cloud VM"
5. After the run, SSH into the VM and run `pm2 ls` — confirm both frontend (Next.js) and backend (uvicorn) processes show `online` status
  </how-to-verify>
  <resume-signal>Type "deploy confirmed" when the Actions run passes and pm2 ls shows services online, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Visual verification of FOUC fix and admin redirect</name>
  <what-built>Plan 10-01 added a blocking script to layout.tsx and updated ThemeProvider to initialize from DOM state. Plan 10-01 also added role guard to admin page.</what-built>
  <how-to-verify>
**FOUC fix:**
1. Start the dev server: `cd frontend && npm run dev`
2. Open http://localhost:3000 in the browser
3. Open DevTools console, run: `localStorage.setItem('theme', 'light')`
4. Hard-reload the page (Ctrl+Shift+R / Cmd+Shift+R)
5. Confirm: page loads in light mode with NO dark flash visible before switching

**Admin redirect:**
6. Log in as a non-admin user (a regular account without `app_metadata.role = 'admin'`)
7. Navigate directly to http://localhost:3000/app/admin
8. Confirm: you are immediately redirected to /app/dashboard without seeing the admin UI
9. Log in as an admin user
10. Navigate to /app/admin — confirm SuggestionQueue renders normally
  </how-to-verify>
  <resume-signal>Type "verified" when both checks pass, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off via resume signals above.
</verification>

<success_criteria>
- GitHub Actions deploy workflow ran successfully (green) on push to main
- VM_HOST, VM_USER, VM_SSH_KEY secrets confirmed set in GitHub repository
- pm2 ls shows services online after deploy
- Light mode hard-reload shows no dark flash
- Non-admin user is redirected from /app/admin to /app/dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/10-cicd-artifacts-polish/10-03-SUMMARY.md`

Also update `.planning/ROADMAP.md` Phase 10 Plans list to mark all plans complete.
</output>
