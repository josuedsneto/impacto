---
phase: 04-mc-simulation
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - frontend/app/app/simulation/page.tsx
autonomous: false
requirements: [SIM-01, SIM-02, SIM-03, SIM-04]

must_haves:
  truths:
    - "Clicking Simular renders the fan chart and metrics without a page reload"
    - "The completed simulation appears in the Histórico tab immediately after completing"
    - "Clicking a past simulation in Histórico replays the exact fan chart from its saved percentiles_series"
    - "Authenticated user only sees their own simulations in the Histórico tab"
  artifacts:
    - path: "frontend/app/app/simulation/page.tsx"
      provides: "Simulation page with two tabs: Simular (form + fan chart + metrics) and Histórico (list of past simulations)"
      contains: "Histórico"
    - path: "frontend/app/app/simulation/page.tsx"
      provides: "Histórico tab fetches GET /api/simulations and displays clickable list"
      contains: "api/simulations"
  key_links:
    - from: "frontend/app/app/simulation/page.tsx"
      to: "frontend/components/simulation/SimulationForm.tsx"
      via: "import SimulationForm"
      pattern: "SimulationForm"
    - from: "frontend/app/app/simulation/page.tsx"
      to: "frontend/components/simulation/FanChart.tsx"
      via: "import FanChart"
      pattern: "FanChart"
    - from: "Histórico tab"
      to: "GET /api/simulations"
      via: "fetch with Bearer token on tab activation"
      pattern: "api/simulations"
---

<objective>
Compose the simulation page: two tabs — Simular (run new simulation) and Histórico (list and replay past simulations).

Purpose: SIM-02 requires the new simulation to appear in Histórico without a reload. SIM-03 requires clicking a past simulation to replay its exact fan chart. SIM-04 is enforced at the API level (plan 01) and surfaced here by showing only what the API returns.
Output: frontend/app/app/simulation/page.tsx
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mc-simulation/04-01-SUMMARY.md
@.planning/phases/04-mc-simulation/04-02-SUMMARY.md
@frontend/app/app/market/page.tsx
@frontend/components/simulation/SimulationForm.tsx
@frontend/components/simulation/FanChart.tsx
@frontend/components/simulation/SimulationMetrics.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend/app/app/simulation/page.tsx</name>
  <files>frontend/app/app/simulation/page.tsx</files>
  <action>
Create `frontend/app/app/simulation/page.tsx` as a Client Component with two tabs.

**Structure:**
```tsx
"use client"

import { useState, useEffect } from "react"
import { createBrowserClient } from "@supabase/ssr"
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import SimulationForm from "@/components/simulation/SimulationForm"
import FanChart from "@/components/simulation/FanChart"
import SimulationMetrics from "@/components/simulation/SimulationMetrics"
```

**State:**
- `activeResult: SimulationResult | null` — the simulation currently displayed (new or replayed)
- `history: HistorySummary[]` — list items from GET /api/simulations (id, ticker, label, p50, created_at)
- `historyLoaded: boolean` — whether history has been fetched at least once
- `activeTab: string` — "simular" | "historico"

**SimulationResult interface** — same fields as the API response (id, ticker, preco_inicial, dias_simulados, num_simulacoes, pct_bound, p5, p20, p25, p50, p75, p80, p95, percentiles_series, label, created_at).

**HistorySummary interface** — id, ticker, label, preco_inicial, dias_simulados, p5, p50, p95, created_at.

**Tab: Simular**
- Renders `<SimulationForm onResult={handleNewResult} />`
- Below the form: if activeResult is set, render `<SimulationMetrics result={activeResult} />` and `<FanChart series={activeResult.percentiles_series} dias_simulados={activeResult.dias_simulados} />`

**handleNewResult(result: SimulationResult):**
1. Set activeResult = result
2. Prepend result to history state (SIM-02: appears immediately without page reload)
   ```ts
   setHistory(prev => [toSummary(result), ...prev])
   ```
3. Where `toSummary` extracts only the HistorySummary fields from the full result

**Tab: Histórico**
- On tab activation (when activeTab changes to "historico" AND historyLoaded === false): fetch GET /api/simulations with Bearer token, set history, set historyLoaded=true
- Renders a list of history items. Each item shows: ticker, label (or "—"), "P50: {p50.toFixed(2)}", date (created_at formatted as pt-BR locale)
- Each item is clickable: on click, fetch GET /api/simulations/{id} with Bearer token, set activeResult = full result, switch activeTab to "simular"
- If history is empty: show "Nenhuma simulação encontrada."

**Bearer token pattern** (follow TickerSuggestForm.tsx pattern):
```ts
const supabase = createBrowserClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)
const { data: { session } } = await supabase.auth.getSession()
const token = session?.access_token ?? ""
```

**API base URL pattern**: `const API = process.env.NEXT_PUBLIC_API_URL ?? ""`

Export as default.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "simulation/page" | head -10</automated>
    <manual>
      1. Run `npm run dev` in frontend/ and navigate to /app/simulation
      2. Fill in the form (ticker=SB=F, accept defaults) and click Simular
      3. Verify fan chart and metrics appear below the form
      4. Click Histórico tab — the completed simulation should appear
      5. Click the simulation item — fan chart replays
    </manual>
  </verify>
  <done>
    frontend/app/app/simulation/page.tsx exists. TypeScript reports no errors for this file. Page renders with two tabs. onResult callback prepends to history. Histórico tab fetches GET /api/simulations on first activation.
  </done>
</task>

</tasks>

<verification>
- `ls frontend/app/app/simulation/` shows page.tsx
- `grep -n "Histórico" frontend/app/app/simulation/page.tsx` shows the tab label
- `grep -n "handleNewResult\|onResult" frontend/app/app/simulation/page.tsx` shows result handler wiring
- `grep -n "api/simulations" frontend/app/app/simulation/page.tsx` shows both list and get-by-id fetches
- `npx tsc --noEmit` from frontend/ reports no errors specific to simulation/page.tsx
</verification>

<success_criteria>
- Page exists at frontend/app/app/simulation/page.tsx with two tabs: Simular and Histórico
- New simulation result renders FanChart + SimulationMetrics immediately in Simular tab
- New simulation is prepended to history state (appears in Histórico without reload)
- Clicking a Histórico item fetches full result and replays fan chart in Simular tab
- GET /api/simulations is scoped by user_id (enforced in plan 04-01; page just displays what the API returns)
</success_criteria>

<output>
After completion, create `.planning/phases/04-mc-simulation/04-03-SUMMARY.md`
</output>
