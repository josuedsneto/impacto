---
phase: 04-mc-simulation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/components/simulation/SimulationForm.tsx
  - frontend/components/simulation/FanChart.tsx
  - frontend/components/simulation/SimulationMetrics.tsx
autonomous: true
requirements: [SIM-01, SIM-02, SIM-03]

must_haves:
  truths:
    - "SimulationForm renders ticker, preco_inicial, dias_simulados, num_simulacoes, pct_bound inputs and a Simular button"
    - "FanChart renders a line chart with p5/p20/p50/p80/p95 bands using percentiles_series data"
    - "SimulationMetrics renders a summary card showing p5, p50, p95 final values"
    - "All three components are exported and ready to import into the simulation page"
  artifacts:
    - path: "frontend/components/simulation/SimulationForm.tsx"
      provides: "Controlled form; on submit calls POST /api/simulations via fetch with Bearer token; returns result to parent via onResult callback"
      min_lines: 60
    - path: "frontend/components/simulation/FanChart.tsx"
      provides: "Recharts AreaChart rendering p5–p95 bands from percentiles_series prop"
      min_lines: 50
    - path: "frontend/components/simulation/SimulationMetrics.tsx"
      provides: "Summary metrics card: p5, p50, p95, ticker, dias_simulados"
      min_lines: 30
  key_links:
    - from: "SimulationForm"
      to: "POST /api/simulations"
      via: "fetch with Authorization: Bearer <token> from createBrowserClient"
      pattern: "api/simulations"
---

<objective>
Build the three reusable React components for the simulation feature: the input form, the fan chart, and the metrics summary.

Purpose: SIM-01 requires the fan chart to be visible after clicking Simular. SIM-02/03 require the same components to work for both new and historical simulations.
Output: Three components in frontend/components/simulation/.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-mc-simulation/04-01-SUMMARY.md
@frontend/components/market/TickerSuggestForm.tsx
@frontend/components/market/PriceChart.tsx
@frontend/app/app/market/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimulationForm and SimulationMetrics components</name>
  <files>
    frontend/components/simulation/SimulationForm.tsx
    frontend/components/simulation/SimulationMetrics.tsx
  </files>
  <action>
Create `frontend/components/simulation/` directory and the two components.

**frontend/components/simulation/SimulationForm.tsx:**
- "use client" directive
- Props: `onResult: (result: SimulationResult) => void` where `SimulationResult` is a locally-defined interface with all fields returned by POST /api/simulations (id, ticker, preco_inicial, dias_simulados, p5, p50, p95, percentiles_series, created_at)
- Form fields (all controlled with useState):
  - ticker (text input, default "SB=F")
  - preco_inicial (number input, default 0, step 0.01)
  - dias_simulados (number input, default 252, min 1, max 1260)
  - num_simulacoes (number input, default 10000, min 100, max 50000)
  - pct_bound (number input, default 0.5, step 0.01, min 0.01, max 1.0)
  - label (text input, optional, placeholder "Nome da simulação (opcional)")
- On submit:
  - Set loading=true, error=null
  - Get access token via `createBrowserClient` from `@supabase/ssr` (follow pattern from TickerSuggestForm.tsx)
  - POST to `/api/simulations` (uses NEXT_PUBLIC_API_URL env var or falls back to "/api" — use `process.env.NEXT_PUBLIC_API_URL ?? ""`)
  - On success: call onResult(data), set loading=false
  - On error: set error to response detail message, set loading=false
- Show error in a red `<p>` below the form
- Simular button shows "Simulando..." when loading=true and is disabled
- Use shadcn/ui components: Button, Input, Label (import from "@/components/ui/...")
- Export as default

**frontend/components/simulation/SimulationMetrics.tsx:**
- "use client" directive
- Props: `result: SimulationResult` (import the interface from SimulationForm or re-define inline)
- Renders a shadcn Card with:
  - Header: "{result.ticker} — {result.dias_simulados} dias"
  - Three stat cells: "P5: {result.p5.toFixed(2)}", "P50 (mediana): {result.p50.toFixed(2)}", "P95: {result.p95.toFixed(2)}"
  - Footer: "Preço inicial: {result.preco_inicial.toFixed(2)}"
- Export as default
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
    <manual>Confirm SimulationForm.tsx and SimulationMetrics.tsx exist in frontend/components/simulation/.</manual>
  </verify>
  <done>
    Both files exist. TypeScript compiler reports no errors for these files (or only pre-existing errors unrelated to the new files).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create FanChart component</name>
  <files>frontend/components/simulation/FanChart.tsx</files>
  <action>
Create `frontend/components/simulation/FanChart.tsx`.

- "use client" directive
- Check if recharts is in frontend/package.json. If not present, note it must be installed — but still write the component assuming it will be added.
- Props: `series: Record<string, number[]>` (the percentiles_series object from the API), `dias_simulados: number`
- Transform series into Recharts data array:
  ```ts
  const data = Array.from({ length: dias_simulados }, (_, i) => ({
    dia: i + 1,
    p5: series.p5[i],
    p20: series.p20[i],
    p50: series.p50[i],
    p80: series.p80[i],
    p95: series.p95[i],
  }))
  ```
- Render a `<ResponsiveContainer width="100%" height={400}>` containing a `<AreaChart data={data}>`:
  - XAxis dataKey="dia", label "Dias"
  - YAxis
  - Tooltip
  - Area for p95–p5 band: use two Area components with `fillOpacity` for shading effect
    - Area key="p95" fill="#93c5fd" fillOpacity={0.3} stroke="#3b82f6" dot={false}
    - Area key="p80" fill="#bfdbfe" fillOpacity={0.3} stroke="none" dot={false}
    - Line (via Area with fill="none") for p50: stroke="#1d4ed8" strokeWidth={2} dot={false}
    - Area key="p20" fill="#bfdbfe" fillOpacity={0.3} stroke="none" dot={false}
    - Area key="p5" fill="#93c5fd" fillOpacity={0.3} stroke="#3b82f6" dot={false}
- Export as default

Add recharts to frontend/package.json dependencies if not already present. Check with:
```bash
grep -c "recharts" frontend/package.json || echo "not found"
```
If not found, add `"recharts": "^2.12.7"` to the dependencies object in frontend/package.json.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "FanChart" | head -10</automated>
    <manual>Confirm FanChart.tsx exists and imports from recharts. Confirm recharts is in package.json if it was missing.</manual>
  </verify>
  <done>
    FanChart.tsx exists, imports ResponsiveContainer/AreaChart from recharts. TypeScript shows no FanChart-specific errors. recharts is present in package.json.
  </done>
</task>

</tasks>

<verification>
- `ls frontend/components/simulation/` lists SimulationForm.tsx, FanChart.tsx, SimulationMetrics.tsx
- `grep -n "onResult" frontend/components/simulation/SimulationForm.tsx` shows the callback prop
- `grep -n "recharts" frontend/components/simulation/FanChart.tsx` shows recharts import
- `grep -n "recharts" frontend/package.json` shows recharts in dependencies
- `npx tsc --noEmit` from frontend/ reports no new errors from these files
</verification>

<success_criteria>
- All three component files created in frontend/components/simulation/
- SimulationForm calls POST /api/simulations with Bearer token and invokes onResult on success
- FanChart renders p5/p20/p50/p80/p95 bands from percentiles_series
- SimulationMetrics shows p5, p50, p95 scalar values
- recharts present in frontend/package.json
</success_criteria>

<output>
After completion, create `.planning/phases/04-mc-simulation/04-02-SUMMARY.md`
</output>
