---
phase: 20-atr-acucar-total-recuperavel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260408000001_atr_usinas.sql
autonomous: true
requirements:
  - ATR-01

must_haves:
  truths:
    - "Tabela usinas existe no Supabase com RLS — apenas admins inserem/atualizam/deletam; usuários associados podem ler"
    - "Tabela atr_simulacoes existe com todos os campos definidos (chuva_mm, impureza_pct, atr_min, atr_esperado, atr_max, producao_total, compartilhado)"
    - "RLS em atr_simulacoes isola simulações por user_id; simulações com compartilhado=true são visíveis a todos da mesma usina"
    - "Admin consegue criar usinas via API sem erros de constraint (nome UNIQUE garante integridade)"
  artifacts:
    - path: "supabase/migrations/20260408000001_atr_usinas.sql"
      provides: "DDL para usinas e atr_simulacoes com índices e políticas RLS"
      contains: "CREATE TABLE.*usinas"
  key_links:
    - from: "atr_simulacoes"
      to: "usinas"
      via: "usina_id FK"
      pattern: "REFERENCES public.usinas"
    - from: "atr_simulacoes"
      to: "auth.users"
      via: "user_id FK ON DELETE CASCADE"
      pattern: "REFERENCES auth.users ON DELETE CASCADE"
---

<objective>
Criar migration Supabase com as tabelas `usinas` e `atr_simulacoes`, índices e políticas RLS adequadas.

Purpose: Persistência centralizada de usinas (gerenciadas por admin) e simulações ATR por usuário/usina.
Output: Arquivo SQL de migration pronto para `supabase db push`.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atr-acucar-total-recuperavel/20-CONTEXT.md

@supabase/migrations/20260406000001_regression_runs.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — tabelas usinas e atr_simulacoes</name>
  <files>supabase/migrations/20260408000001_atr_usinas.sql</files>
  <action>
Criar arquivo de migration SQL com o seguinte schema:

**Tabela `usinas`:**
```sql
CREATE TABLE IF NOT EXISTS public.usinas (
  id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  nome  TEXT NOT NULL UNIQUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```
Lista de usinas é fixa, gerenciada por admin — sem user_id na tabela. RLS: qualquer usuário autenticado pode fazer SELECT; apenas service_role (backend FastAPI com SUPABASE_SERVICE_ROLE_KEY) faz INSERT/UPDATE/DELETE.

**Tabela `user_usinas`** (associação admin-managed):
```sql
CREATE TABLE IF NOT EXISTS public.user_usinas (
  user_id  UUID NOT NULL REFERENCES auth.users ON DELETE CASCADE,
  usina_id UUID NOT NULL REFERENCES public.usinas ON DELETE CASCADE,
  PRIMARY KEY (user_id, usina_id)
);
```
RLS: SELECT permitido para auth.uid() = user_id (usuário vê suas próprias associações). INSERT/UPDATE/DELETE apenas via service_role.

**Tabela `atr_simulacoes`:**
```sql
CREATE TABLE IF NOT EXISTS public.atr_simulacoes (
  id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id        UUID NOT NULL REFERENCES auth.users ON DELETE CASCADE,
  usina_id       UUID NOT NULL REFERENCES public.usinas ON DELETE CASCADE,
  chuva_mm       NUMERIC(8,2) NOT NULL,
  impureza_pct   NUMERIC(6,3) NOT NULL,
  atr_min        NUMERIC(8,3) NOT NULL,
  atr_esperado   NUMERIC(8,3) NOT NULL,
  atr_max        NUMERIC(8,3) NOT NULL,
  producao_total NUMERIC(14,2),
  compartilhado  BOOLEAN NOT NULL DEFAULT false,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**Índices:**
```sql
CREATE INDEX IF NOT EXISTS idx_atr_simulacoes_user_id  ON public.atr_simulacoes (user_id);
CREATE INDEX IF NOT EXISTS idx_atr_simulacoes_usina_id ON public.atr_simulacoes (usina_id);
CREATE INDEX IF NOT EXISTS idx_user_usinas_user_id     ON public.user_usinas (user_id);
```

**RLS `usinas`** — leitura aberta a autenticados, escrita bloqueada (service_role bypassa RLS):
```sql
ALTER TABLE public.usinas ENABLE ROW LEVEL SECURITY;
CREATE POLICY "usinas: authenticated read"
  ON public.usinas FOR SELECT
  USING (auth.role() = 'authenticated');
```

**RLS `user_usinas`**:
```sql
ALTER TABLE public.user_usinas ENABLE ROW LEVEL SECURITY;
CREATE POLICY "user_usinas: users see own rows"
  ON public.user_usinas FOR SELECT
  USING (auth.uid() = user_id);
```

**RLS `atr_simulacoes`** — usuário vê as próprias + as compartilhadas da mesma usina:
```sql
ALTER TABLE public.atr_simulacoes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "atr_simulacoes: users see own rows"
  ON public.atr_simulacoes FOR SELECT
  USING (
    auth.uid() = user_id
    OR (
      compartilhado = true
      AND usina_id IN (
        SELECT usina_id FROM public.user_usinas WHERE user_id = auth.uid()
      )
    )
  );

CREATE POLICY "atr_simulacoes: users insert own rows"
  ON public.atr_simulacoes FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "atr_simulacoes: users update own rows"
  ON public.atr_simulacoes FOR UPDATE
  USING (auth.uid() = user_id);
```

Seguir exatamente o padrão do arquivo `supabase/migrations/20260406000001_regression_runs.sql` para nomenclatura de políticas e ordem das instruções.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE|ENABLE ROW LEVEL SECURITY|CREATE POLICY|CREATE INDEX" "supabase/migrations/20260408000001_atr_usinas.sql" | wc -l</automated>
    <manual>Verificar que o arquivo SQL tem tabelas usinas, user_usinas e atr_simulacoes com seus índices e políticas RLS. O output do grep deve ser >= 12 linhas.</manual>
    <sampling_rate>after this task, before proceeding</sampling_rate>
  </verify>
  <done>Arquivo SQL de migration existe com 3 tabelas (usinas, user_usinas, atr_simulacoes), índices em user_id/usina_id e políticas RLS que isolam simulações por user e permitem ver compartilhadas da mesma usina.</done>
</task>

</tasks>

<verification>
- `supabase/migrations/20260408000001_atr_usinas.sql` existe e contém DDL válido
- 3 tabelas criadas com IF NOT EXISTS
- RLS habilitado nas 3 tabelas
- Políticas corretas: usinas (read-only autenticados), user_usinas (ver próprias), atr_simulacoes (próprias + compartilhadas mesma usina)
</verification>

<success_criteria>
Migration SQL pronta para aplicar via `supabase db push` sem erros. Schema suporta o fluxo completo: admin cria usinas, associa usuários, usuários salvam simulações privadas ou publicam para a usina.
</success_criteria>

<output>
After completion, create `.planning/phases/20-atr-acucar-total-recuperavel/20-01-SUMMARY.md`
</output>
