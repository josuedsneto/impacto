---
phase: 16-correcoes-pontuais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/06_Volatilidade.py
autonomous: true
requirements:
  - VOL-01

must_haves:
  truths:
    - "A página exibe volatilidade DIÁRIA e ANUALIZADA separadamente, com labels claros em português"
    - "A métrica anualizada é calculada corretamente multiplicando a diária por sqrt(252)"
    - "O título dos gráficos indica se é diária ou anualizada"
    - "Os valores exibidos nos st.write() indicam explicitamente a base temporal (diária / anualizada)"
  artifacts:
    - path: "pages/06_Volatilidade.py"
      provides: "Página de Volatilidade com EWMA e GARCH diário e anualizado"
      contains: "sqrt(252)"
  key_links:
    - from: "pages/06_Volatilidade.py"
      to: "data['EWMA Volatility']"
      via: "multiplicação por np.sqrt(252)"
      pattern: "sqrt\\(252\\)"
---

<objective>
Auditar e corrigir a página Volatilidade para deixar explícito se os valores exibidos são diários ou anualizados. O cliente precisa saber claramente qual parâmetro está vendo.

Purpose: O cliente sinalizou confusão sobre se a volatilidade exibida é diária ou anualizada. A distinção é crítica para uso em modelos de precificação.
Output: pages/06_Volatilidade.py corrigido com labels e colunas adicionais de volatilidade anualizada.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/06_Volatilidade.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar volatilidade anualizada e clarificar labels na página Volatilidade</name>
  <files>pages/06_Volatilidade.py</files>
  <action>
Editar `pages/06_Volatilidade.py` para:

1. Na função `get_historical_data()`, após calcular `EWMA Volatility` e `GARCH Volatility`, adicionar colunas anualizadas:
   ```python
   data['EWMA Volatility Anualizada'] = data['EWMA Volatility'] * np.sqrt(252)
   data['GARCH Volatility Anualizada'] = data['GARCH Volatility'] * np.sqrt(252)
   ```
   A EWMA usa `pct_change().ewm(span=20).std()` — isso é DIÁRIO (sem fator de escala). A GARCH usa `conditional_volatility / 100` que também é DIÁRIA (o modelo arch escala por 100 via `* 100` antes do fit). Ambas são diárias.

2. No bloco de exibição após o botão "Calcular", substituir os dois gráficos atuais por gráficos que mostram AMBAS (diária e anualizada) no mesmo layout:

   Para EWMA:
   - Gráfico: `title='Volatilidade EWMA - {variable} (Diária)'`, coluna `EWMA Volatility`
   - Métrica: `st.write(f"- **Volatilidade Média EWMA (Diária):** {ewma_vol_mean:.4%}")`
   - Adicionar: `ewma_vol_anual = data['EWMA Volatility Anualizada'].mean()`
   - Gráfico: `title='Volatilidade EWMA - {variable} (Anualizada)'`, coluna `EWMA Volatility Anualizada`
   - Métrica: `st.write(f"- **Volatilidade Média EWMA (Anualizada):** {ewma_vol_anual:.4%}")`

   Para GARCH:
   - Gráfico: `title='Volatilidade Condicional GARCH - {variable} (Diária)'`, coluna `GARCH Volatility`
   - Métrica: `st.write(f"- **Volatilidade Média GARCH (Diária):** {garch_vol_mean:.4%}")`
   - Adicionar: `garch_vol_anual = data['GARCH Volatility Anualizada'].mean()`
   - Gráfico: `title='Volatilidade Condicional GARCH - {variable} (Anualizada)'`, coluna `GARCH Volatility Anualizada`
   - Métrica: `st.write(f"- **Volatilidade Média GARCH (Anualizada):** {garch_vol_anual:.4%}")`

3. Adicionar `st.info("Volatilidade diária calculada a partir de retornos diários. Anualizada = Diária × √252 (dias úteis/ano).")` logo após o título da página.

Manter toda a lógica existente intacta (GARCH params, download Excel, etc.). Não remover nenhuma funcionalidade.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast, sys; ast.parse(open('pages/06_Volatilidade.py').read()); print('syntax OK')" && grep -c "sqrt(252)" pages/06_Volatilidade.py</automated>
    <manual>Rodar `streamlit run Painel.py`, navegar para Volatilidade, selecionar Açúcar, clicar Calcular — confirmar que aparecem 4 gráficos (EWMA diária, EWMA anualizada, GARCH diária, GARCH anualizada) com labels corretos e o st.info explicativo.</manual>
  </verify>
  <done>Arquivo sem erros de sintaxe; grep retorna 2 ocorrências de sqrt(252) (uma para EWMA, uma para GARCH); gráficos e métricas exibem base temporal explicitamente.</done>
</task>

</tasks>

<verification>
python -c "import ast; ast.parse(open('pages/06_Volatilidade.py').read()); print('OK')"
grep "sqrt(252)" pages/06_Volatilidade.py
grep -E "Diária|Anualizada" pages/06_Volatilidade.py
</verification>

<success_criteria>
- Página Volatilidade exibe 4 gráficos: EWMA diária, EWMA anualizada, GARCH diária, GARCH anualizada
- Labels de todos os st.write() e títulos de gráficos indicam explicitamente "(Diária)" ou "(Anualizada)"
- Fórmula de anualização usa sqrt(252) conforme convenção de mercado
- st.info() explica a metodologia ao usuário
- Nenhuma funcionalidade existente removida (GARCH params, download Excel)
</success_criteria>

<output>
After completion, create `.planning/phases/16-export-csv-pdf/16-correcoes-pontuais-01-SUMMARY.md`
</output>
