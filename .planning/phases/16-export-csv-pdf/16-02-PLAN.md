---
phase: 16-correcoes-pontuais
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/12_Breakeven.py
  - pages/13_Black_Scholes.py
autonomous: true
requirements:
  - BREAK-01
  - BS-01

must_haves:
  truths:
    - "A página Breakeven aceita campos 'Gasto Fixo Total' e 'Gasto Variável por Unidade' configuráveis pelo usuário"
    - "Os valores hardcoded de custo fixo e variável são substituídos pelos inputs do usuário no cálculo"
    - "A página Black-Scholes usa um contrato de açúcar NY válido (não expirado) com vencimento futuro"
    - "Black-Scholes calcula T corretamente para o contrato selecionado e exibe erro se já expirou"
  artifacts:
    - path: "pages/12_Breakeven.py"
      provides: "Análise de breakeven com gastos configuráveis"
      contains: "gasto_fixo"
    - path: "pages/13_Black_Scholes.py"
      provides: "Precificação Black-Scholes com contrato atualizado"
      contains: "SBK27"
  key_links:
    - from: "pages/12_Breakeven.py"
      to: "função custo()"
      via: "variáveis gasto_fixo_total e gasto_variavel_por_unidade nos st.number_input"
      pattern: "gasto_fixo_total"
    - from: "pages/13_Black_Scholes.py"
      to: "assets dict"
      via: "ticker SBK27.NYB com datetime(2027, ...)"
      pattern: "SBK27"
---

<objective>
Corrigir duas páginas independentes: (1) Breakeven — substituir valores hardcoded de custo por campos de input configuráveis; (2) Black-Scholes — atualizar o contrato de açúcar NY para um vencimento futuro válido.

Purpose: O cliente precisa ajustar os gastos no Breakeven sem editar código. O contrato SBK26 expira em 2026-04-30 (23 dias a partir de hoje 2026-04-07), tornando a página inutilizável em breve.
Output: pages/12_Breakeven.py e pages/13_Black_Scholes.py corrigidos e funcionais.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/12_Breakeven.py
@pages/13_Black_Scholes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campos de Gasto Fixo e Gasto Variável na página Breakeven</name>
  <files>pages/12_Breakeven.py</files>
  <action>
Editar `pages/12_Breakeven.py` para:

1. Após a linha `variavel_parametro = st.selectbox(...)` e antes do loop `for variavel in [...]`, adicionar dois novos inputs:
   ```python
   st.subheader("Parâmetros de Custo")
   gasto_fixo_total = st.number_input(
       "Gasto Fixo Total (R$):",
       value=152723235.0,
       step=1000000.0,
       format="%.0f",
       help="Soma dos custos fixos independentes do volume produzido"
   )
   gasto_variavel_por_unidade = st.number_input(
       "Gasto Variável por Unidade (R$/unidade):",
       value=0.0,
       step=1000.0,
       format="%.2f",
       help="Custo adicional por unidade produzida (aplicado sobre a produção variável)"
   )
   ```
   O valor default `152723235.0` é a soma dos hardcoded atuais: `88704735 + 43732035 + 20286465 = 152723235`.

2. Na função `custo()`, os três últimos termos hardcoded (`88704735 + 43732035 + 20286465`) representam gastos fixos. Refatorar a função para receber `gasto_fixo_total` e `gasto_variavel_por_unidade` como parâmetros adicionais:

   Alterar a assinatura: `def custo(variavel_parametro, valor_parametro, outras_variaveis, gasto_fixo_total, gasto_variavel_por_unidade):`

   Substituir as três ocorrências de `88704735 + 43732035 + 20286465` por `gasto_fixo_total`.

   Para `gasto_variavel_por_unidade`: adicionar `+ gasto_variavel_por_unidade * valor_parametro` ao retorno em cada branch da função `custo()`. Isso aplica o custo variável sobre a variável paramétrica (NY, Câmbio, Prod VHP, etc.).

3. No loop de cálculo:
   ```python
   custos.append(custo(variavel_parametro, valor_parametro, outras_variaveis, gasto_fixo_total, gasto_variavel_por_unidade))
   ```

Não alterar a função `faturamento()`. Manter toda a lógica do gráfico e cálculo do ponto de break-even intactos.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast; ast.parse(open('pages/12_Breakeven.py').read()); print('syntax OK')" && grep -c "gasto_fixo_total" pages/12_Breakeven.py</automated>
    <manual>Rodar a página Breakeven, verificar que aparecem os dois novos campos "Gasto Fixo Total" e "Gasto Variável por Unidade" com os valores default, e que ao clicar "Gerar Gráfico" o resultado é o mesmo do comportamento anterior (valores default reproduzem o hardcoded original).</manual>
  </verify>
  <done>Arquivo sem erros de sintaxe; grep retorna ao menos 3 ocorrências de "gasto_fixo_total" (definição do input + 3 branches da função custo); campos visíveis na UI com valores default corretos.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar contrato e volatilidade na página Black-Scholes</name>
  <files>pages/13_Black_Scholes.py</files>
  <action>
Editar `pages/13_Black_Scholes.py` para:

1. Atualizar o dicionário `assets` — trocar `SBK26.NYB` (vencimento 2026-04-30, expira em dias) pelo próximo contrato ativo. Os contratos NY #11 seguem o padrão: K=Maio, N=Julho, V=Outubro, H=Março. Após SBK26 (Maio/2026), o próximo é:
   - `SBN26.NYB` — Julho 2026, vencimento aproximado `datetime(2026, 6, 30)`

   Se quiser oferecer mais contratos, incluir também:
   - `SBV26.NYB` — Outubro 2026, vencimento aproximado `datetime(2026, 9, 30)`

   Atualizar o dicionário assim:
   ```python
   assets = {
       'SBN26.NYB': datetime(2026, 6, 30),
       'SBV26.NYB': datetime(2026, 9, 30),
   }
   volatilities = {
       'SBN26.NYB': 0.2573,
       'SBV26.NYB': 0.2573,
   }
   ```
   Manter o mesmo valor de volatilidade `0.2573` como default — o cliente pode ajustar se necessário. O risco-livre `risk_free_rate = 0.053` permanece inalterado.

2. Também atualizar a faixa de strikes na tabela de preços (linha `strikes = np.arange(16, 22.25, 0.25)`) para um range mais adequado centrado no preço atual de açúcar (~17-18 cents):
   ```python
   strikes = np.arange(14, 24.25, 0.25)
   ```
   Isso garante que o preço de mercado atual caia dentro do range visualizado.

3. Adicionar um `st.info()` explicativo após o `st.title()`:
   ```python
   st.info("Volatilidade implícita usada: 25.73% (valor fixo). Atualize o campo de volatilidade conforme dados de mercado.")
   ```
   Na verdade, melhor expor a volatilidade como input editável pelo usuário. Substituir a linha `sigma = volatilities[asset]` por:
   ```python
   sigma = st.number_input(
       "Volatilidade implícita (anualizada):",
       value=volatilities[asset],
       min_value=0.01,
       max_value=2.0,
       step=0.01,
       format="%.4f",
       help="Volatilidade anualizada usada no modelo Black-Scholes. Padrão: 25.73%"
   )
   ```
   Remover a linha `sigma = volatilities[asset]` do bloco `if st.button("Simular"):` — o sigma agora é calculado antes do botão.

Manter toda a lógica Black-Scholes, gráficos e estrutura da página intactos.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast; ast.parse(open('pages/13_Black_Scholes.py').read()); print('syntax OK')" && grep "SBN26" pages/13_Black_Scholes.py</automated>
    <manual>Rodar a página Black-Scholes — confirmar que o seletor de ativo mostra SBN26.NYB e SBV26.NYB (não SBK26), que o input de volatilidade aparece editável, e que ao clicar Simular a página calcula sem erro "contrato expirado".</manual>
  </verify>
  <done>Arquivo sem erros de sintaxe; grep encontra "SBN26" no arquivo; contrato SBK26 removido; campo de volatilidade editável presente antes do botão Simular.</done>
</task>

</tasks>

<verification>
python -c "import ast; ast.parse(open('pages/12_Breakeven.py').read()); print('Breakeven OK')"
python -c "import ast; ast.parse(open('pages/13_Black_Scholes.py').read()); print('BS OK')"
grep "gasto_fixo_total" pages/12_Breakeven.py
grep "SBN26" pages/13_Black_Scholes.py
grep -v "SBK26" pages/13_Black_Scholes.py | grep -c "SB"
</verification>

<success_criteria>
- Breakeven: dois novos inputs aparecem na UI com defaults que reproduzem o comportamento anterior; função custo() aceita gasto_fixo_total e gasto_variavel_por_unidade; hardcoded 152723235 substituído
- Black-Scholes: contrato SBK26 removido; SBN26.NYB e SBV26.NYB disponíveis no seletor; sigma editável antes do botão; nenhum erro de "contrato expirado" ao rodar hoje (2026-04-07)
- Ambos os arquivos passam em verificação de sintaxe Python
</success_criteria>

<output>
After completion, create `.planning/phases/16-export-csv-pdf/16-correcoes-pontuais-02-SUMMARY.md`
</output>
