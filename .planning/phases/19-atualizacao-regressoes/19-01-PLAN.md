---
phase: 19-atualizacao-regressoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/04_Regressão_Dólar.py
  - backend/regression.py
autonomous: true
requirements:
  - REG-01

must_haves:
  truths:
    - "Página Regressão Dólar busca dados históricos via BCB SGS e FRED (não lê dadosReg.xls)"
    - "Inputs padrão refletem valores atuais: Selic ~13.75%, Fed Funds ~4.33%, M2 BCB e FRED recentes"
    - "Modelo OLS treina sobre janela de 60 meses de dados reais e exibe R², RMSE e correlação"
    - "Previsão da taxa de câmbio é exibida junto com heatmap de correlação e gráfico real vs previsto"
  artifacts:
    - path: "pages/04_Regressão_Dólar.py"
      provides: "Streamlit page — Regressão Dólar atualizada com dados live"
    - path: "backend/regression.py"
      provides: "fetch_dolar_history janela ampliada e defaults corrigidos"
  key_links:
    - from: "pages/04_Regressão_Dólar.py"
      to: "backend/regression.py (run_dolar_regression)"
      via: "Streamlit page reimplementa localmente a mesma lógica de fetch BCB+FRED"
      pattern: "SGS\\(\\)|fetch_fred"
---

<objective>
Atualizar a página Streamlit Regressão Dólar para usar dados live (BCB SGS + FRED + yfinance USDBRL=X) em vez do arquivo local `dadosReg.xls`, alinhar os valores padrão dos inputs com o estado atual dos indicadores, e revisar a janela temporal de treino do modelo OLS no backend.

Purpose: O arquivo dadosReg.xls é estático e desatualizado. A página precisa buscar os mesmos dados que o backend FastAPI já busca via BCB SGS (séries 432=Selic, 1837=M2, 21859=ProdInd) e FRED (FEDFUNDS, M2SL, INDPRO), garantindo que modelo e previsão reflitam dados recentes.

Output: pages/04_Regressão_Dólar.py atualizado + backend/regression.py com janela e defaults revisados.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/regression.py
@pages/04_Regressão_Dólar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revisar janela temporal e defaults do backend dólar</name>
  <files>backend/regression.py</files>
  <action>
Em `backend/regression.py`, revisar:

1. Em `fetch_dolar_history(months: int = 60)`: ampliar para `months: int = 72` (6 anos de histórico mensal) para capturar mais ciclos de juros. Nenhuma outra mudança estrutural — a lógica de merge BCB+FRED+yfinance já está correta.

2. Em `_USDA_DEFAULTS` — NÃO alterar aqui (pertencem à Regressão Açúcar). Verificar apenas que `_BCB_SERIES` e `_FRED_SERIES` estão corretos:
   - `_BCB_SERIES`: selic=432, m2_bcb=1837, prod_industrial=21859 — manter.
   - `_FRED_SERIES`: fed_funds="FEDFUNDS", m2_fred="M2SL", indpro="INDPRO" — manter.

3. Em `get_dolar_defaults()`: ampliar o lookback de 90 para 120 dias para garantir que séries com publicação mensal (BCB industrial, FRED INDPRO) tenham ao menos um valor recente. Mudar `days=90` → `days=120`.

4. Adicionar comentário acima de `fetch_dolar_history` indicando janela atual:
   ```python
   # Janela padrão: 72 meses (~6 anos) para capturar ciclos de juros BR e EUA
   ```
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/backend && python -c "from regression import get_dolar_defaults, fetch_dolar_history; d = get_dolar_defaults(); print('defaults:', d); df = fetch_dolar_history(months=12); print('rows:', len(df), 'cols:', list(df.columns))"</automated>
    <manual>Confirmar que defaults retorna valores numéricos (não todos None) e que o DataFrame tem colunas taxa_dolar, selic, m2_bcb, prod_industrial, fed_funds, m2_fred, indpro</manual>
  </verify>
  <done>fetch_dolar_history usa janela padrão de 72 meses; get_dolar_defaults usa lookback de 120 dias; módulo importa sem erros</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar página Streamlit Regressão Dólar para dados live</name>
  <files>pages/04_Regressão_Dólar.py</files>
  <action>
Reescrever `pages/04_Regressão_Dólar.py` para substituir a dependência do arquivo `dadosReg.xls` por busca live via BCB SGS e FRED, alinhando com a lógica do `backend/regression.py`. Manter a estrutura Streamlit existente (require_login, show_logo, plotly charts).

Estrutura do novo arquivo:

```python
import streamlit as st
import numpy as np
import pandas as pd
import plotly.graph_objs as go
import matplotlib.pyplot as plt
import seaborn as sns
from datetime import date, timedelta

from utils import require_login, show_logo

st.set_page_config(page_title="Regressão Dólar", page_icon="📈", layout="wide")
require_login()
show_logo()

# ── Constantes ────────────────────────────────────────────────────────────────
_BCB_SERIES = {"selic": 432, "m2_bcb": 1837, "prod_industrial": 21859}
_FRED_SERIES = {"fed_funds": "FEDFUNDS", "m2_fred": "M2SL", "indpro": "INDPRO"}
_FRED_BASE = "https://api.stlouisfed.org/fred/series/observations"
_FEATURE_COLS = ["selic", "m2_bcb", "prod_industrial", "fed_funds", "m2_fred", "indpro"]

# ── Funções de fetch ──────────────────────────────────────────────────────────
@st.cache_data(ttl=3600)
def fetch_dados_dolar(meses: int = 72) -> pd.DataFrame:
    """
    Busca histórico mensal: BCB SGS (Selic, M2, ProdInd) + FRED (FedFunds, M2SL, INDPRO)
    + USDBRL=X via yfinance. Retorna DataFrame com colunas:
    taxa_dolar, selic, m2_bcb, prod_industrial, fed_funds, m2_fred, indpro
    """
    import yfinance as yf
    import requests as req

    today = date.today()
    start = today - timedelta(days=meses * 31)
    start_str = start.strftime("%Y-%m-%d")

    # BCB
    bcb_df = pd.DataFrame()
    try:
        from bcb import SGS
        sgs = SGS()
        raw = sgs.get(list(_BCB_SERIES.values()), start=start, end=today)
        for key, code in _BCB_SERIES.items():
            if code in raw.columns:
                series = raw[code].dropna().resample("ME").last()
                series.index = series.index.strftime("%Y-%m")
                bcb_df[key] = series
    except Exception as e:
        st.warning(f"BCB indisponível: {e}")

    # FRED
    fred_api_key = st.secrets.get("FRED_API_KEY", "")
    fred_df = pd.DataFrame()
    if fred_api_key:
        for key, sid in _FRED_SERIES.items():
            try:
                resp = req.get(_FRED_BASE, params={
                    "series_id": sid, "api_key": fred_api_key,
                    "observation_start": start_str, "frequency": "m", "file_type": "json"
                }, timeout=15)
                obs = resp.json().get("observations", [])
                records = [{"period": o["date"][:7], "value": float(o["value"])}
                           for o in obs if o.get("value", ".") != "."]
                if records:
                    s = pd.DataFrame(records).set_index("period")["value"]
                    fred_df[key] = s
            except Exception as e:
                st.warning(f"FRED {sid} indisponível: {e}")
    else:
        st.info("FRED_API_KEY não configurada — variáveis FRED não incluídas no modelo.")

    # yfinance USDBRL=X
    usdbrl_df = pd.DataFrame()
    try:
        raw_usdbrl = yf.download("USDBRL=X", start=start_str,
                                 interval="1mo", progress=False, auto_adjust=True)
        if not raw_usdbrl.empty:
            close = raw_usdbrl["Close"].dropna()
            close.index = pd.DatetimeIndex(close.index).strftime("%Y-%m")
            usdbrl_df["taxa_dolar"] = close
    except Exception as e:
        st.warning(f"yfinance USDBRL=X indisponível: {e}")

    merged = usdbrl_df.copy()
    for df in [bcb_df, fred_df]:
        if not df.empty:
            merged = merged.join(df, how="left")
    return merged.dropna()


def obter_defaults_atuais() -> dict:
    """Busca últimos valores das séries para pré-preencher inputs."""
    try:
        from bcb import SGS
        sgs = SGS()
        today = date.today()
        start = today - timedelta(days=120)
        raw = sgs.get(list(_BCB_SERIES.values()), start=start, end=today)
        defaults = {}
        for key, code in _BCB_SERIES.items():
            if code in raw.columns:
                col = raw[code].dropna()
                defaults[key] = float(col.iloc[-1]) if not col.empty else None
            else:
                defaults[key] = None
        return defaults
    except Exception:
        return {k: None for k in _BCB_SERIES}
```

**Seção principal (abaixo das funções):**

```python
st.title("Previsão da Taxa de Câmbio (USD/BRL)")
st.write("Modelo OLS treinado em dados históricos BCB + FRED. Insira as premissas e clique em **Gerar Regressão**.")

# Pré-preencher com defaults atuais
with st.spinner("Buscando valores atuais das séries..."):
    defaults = obter_defaults_atuais()

col1, col2, col3 = st.columns(3)
with col1:
    selic_proj = st.number_input("Selic (% a.a.)", value=float(defaults.get("selic") or 13.75), step=0.25)
    m2_bcb_proj = st.number_input("M2 BCB (R$ bi)", value=float(defaults.get("m2_bcb") or 5200.0), step=10.0)
with col2:
    prod_ind_br_proj = st.number_input("Prod. Industrial BCB (índice)", value=float(defaults.get("prod_industrial") or 105.0), step=0.1)
    fed_funds_proj = st.number_input("Fed Funds (% a.a.)", value=4.33, step=0.25)
with col3:
    m2_fred_proj = st.number_input("M2 EUA (bi USD)", value=21000.0, step=100.0)
    indpro_proj = st.number_input("Prod. Industrial EUA (índice)", value=103.0, step=0.1)

if st.button("Gerar Regressão"):
    import statsmodels.api as sm
    from sklearn.metrics import mean_squared_error, r2_score

    with st.spinner("Buscando dados históricos e treinando modelo..."):
        df = fetch_dados_dolar(meses=72)

    if len(df) < 24:
        st.error(f"Dados insuficientes: {len(df)} meses após merge (mínimo 24 necessário).")
        st.stop()

    # Determinar features disponíveis (FRED pode estar ausente se sem API key)
    feature_cols = [c for c in _FEATURE_COLS if c in df.columns]
    y = df["taxa_dolar"].values
    X = df[feature_cols].values

    X_const = sm.add_constant(X, has_constant="add")
    fitted = sm.OLS(endog=y, exog=X_const).fit()
    y_pred = fitted.fittedvalues
    r2 = float(fitted.rsquared)
    rmse = float(np.sqrt(mean_squared_error(y, y_pred)))

    # Previsão com inputs do usuário
    input_dict = {
        "selic": selic_proj, "m2_bcb": m2_bcb_proj,
        "prod_industrial": prod_ind_br_proj, "fed_funds": fed_funds_proj,
        "m2_fred": m2_fred_proj, "indpro": indpro_proj,
    }
    input_row = pd.DataFrame([[input_dict[c] for c in feature_cols]], columns=feature_cols)
    input_const = sm.add_constant(input_row, has_constant="add")
    taxa_prevista = float(fitted.predict(input_const)[0])

    st.success(f"**Taxa de câmbio prevista: R$ {taxa_prevista:.4f}**")
    col_m1, col_m2, col_m3 = st.columns(3)
    col_m1.metric("R²", f"{r2:.4f}")
    col_m2.metric("RMSE", f"{rmse:.4f}")
    col_m3.metric("Obs. de treino", str(len(df)))

    # Heatmap de correlação
    corr_df = df[["taxa_dolar"] + feature_cols]
    fig_corr, ax = plt.subplots(figsize=(10, 7))
    sns.heatmap(corr_df.corr(), annot=True, cmap="coolwarm", fmt=".2f", linewidths=0.5, ax=ax)
    ax.set_title("Matriz de Correlação")
    st.pyplot(fig_corr)
    plt.close(fig_corr)

    # Gráfico real vs previsto
    fig = go.Figure()
    fig.add_trace(go.Scatter(x=list(df.index), y=list(y), mode="lines", name="Real"))
    fig.add_trace(go.Scatter(x=list(df.index), y=list(y_pred), mode="lines", name="Previsto", line=dict(dash="dash")))
    fig.update_layout(title="Taxa de Câmbio: Real vs Previsto", xaxis_title="Período", yaxis_title="USD/BRL")
    st.plotly_chart(fig, use_container_width=True)

    # Coeficientes do modelo
    st.subheader("Coeficientes do Modelo OLS")
    coef_names = ["const"] + feature_cols
    coef_df = pd.DataFrame({
        "Variável": coef_names,
        "Coeficiente": [round(float(c), 6) for c in fitted.params],
        "P-valor": [round(float(p), 4) for p in fitted.pvalues],
    })
    st.dataframe(coef_df, use_container_width=True)
```

Não importar `matplotlib.pyplot as plt` duas vezes. Remover o import `import plotly.subplots as sp` que não é usado. Preservar os imports `from utils import require_login, show_logo`.

Nota sobre FRED_API_KEY: na Streamlit app, buscar de `st.secrets.get("FRED_API_KEY", "")` — no `.streamlit/secrets.toml` adicionar a chave se disponível. Se ausente, o modelo roda apenas com séries BCB + yfinance (sem FRED) e exibe um `st.info`.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast, sys; ast.parse(open('pages/04_Regressão_Dólar.py').read()); print('syntax OK')"</automated>
    <manual>Rodar `streamlit run Painel.py` e navegar para Regressão Dólar. Verificar que a página carrega sem ImportError, busca valores padrão e ao clicar em "Gerar Regressão" exibe R², RMSE, heatmap de correlação e gráfico real vs previsto usando dados live (não dadosReg.xls).</manual>
  </verify>
  <done>pages/04_Regressão_Dólar.py não referencia dadosReg.xls; busca dados via BCB SGS + FRED + yfinance; inputs pré-preenchidos com valores atuais; modelo OLS exibe R², RMSE, coeficientes, heatmap e gráfico</done>
</task>

</tasks>

<verification>
1. `python -c "from regression import get_dolar_defaults, fetch_dolar_history; print('OK')"` — sem ImportError
2. `python -c "import ast; ast.parse(open('pages/04_Regressão_Dólar.py').read()); print('syntax OK')"` — sintaxe válida
3. Grep confirma ausência de "dadosReg.xls" em pages/04_Regressão_Dólar.py
4. Grep confirma presença de "BCB" ou "SGS" em pages/04_Regressão_Dólar.py
</verification>

<success_criteria>
- pages/04_Regressão_Dólar.py não lê mais dadosReg.xls
- Dados de treino vêm de BCB SGS (séries 432, 1837, 21859) + yfinance USDBRL=X (+ FRED se FRED_API_KEY disponível)
- Janela padrão de treino no backend é 72 meses
- Inputs pré-preenchidos com valores recentes (Selic ~13.75% reflete ciclo atual)
- Modelo exibe: taxa prevista, R², RMSE, heatmap de correlação, gráfico real vs previsto, tabela de coeficientes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-atualizacao-regressoes/19-01-SUMMARY.md`
</output>
