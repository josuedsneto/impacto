---
phase: 19-atualizacao-regressoes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/05_Regressão_Açúcar.py
  - backend/regression.py
autonomous: true
requirements:
  - REG-02

must_haves:
  truths:
    - "Página Regressão Açúcar não lê mais dadosRegSugar.xlsx — busca dados via yfinance (SB=F, USDBRL=X, CL=F)"
    - "Dados USDA incluem ano 2025 (safra 2024/25) com valores atualizados"
    - "Inputs padrão refletem valores de mercado atuais: CL=F ~70-80, USDBRL ~5.8-6.0"
    - "Modelo Ridge ou RandomForest exibe R², RMSE, preço previsto SB=F com intervalo de confiança"
    - "Correlação entre variáveis é exibida como heatmap"
  artifacts:
    - path: "pages/05_Regressão_Açúcar.py"
      provides: "Streamlit page — Regressão Açúcar atualizada com dados live"
    - path: "backend/regression.py"
      provides: "_USDA_ANNUAL com ano 2025 adicionado + _USDA_DEFAULTS revisados"
  key_links:
    - from: "pages/05_Regressão_Açúcar.py"
      to: "yfinance (SB=F, USDBRL=X, CL=F)"
      via: "yf.download com interval=1y"
      pattern: "yf\\.download"
    - from: "backend/regression.py (_USDA_ANNUAL)"
      to: "run_acucar_regression"
      via: "fetch_acucar_history() merge"
      pattern: "_USDA_ANNUAL"
---

<objective>
Atualizar a página Streamlit Regressão Açúcar para usar dados live via yfinance (não dadosRegSugar.xlsx) e adicionar o ano safra 2025 aos dados USDA hardcoded no backend. Revisar valores padrão dos inputs e alinhar unidades (MMT em vez de "milhares").

Purpose: O arquivo dadosRegSugar.xlsx é estático e os dados USDA terminam em 2024. Com a safra 2024/25 encerrada, é necessário adicionar os dados USDA de 2025 para manter o modelo relevante. A página Streamlit também precisa buscar dados live de preço como o backend já faz.

Output: pages/05_Regressão_Açúcar.py atualizado + backend/regression.py com _USDA_ANNUAL e _USDA_DEFAULTS atualizados para 2025.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/regression.py
@pages/05_Regressão_Açúcar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar ano 2025 ao _USDA_ANNUAL e revisar defaults no backend</name>
  <files>backend/regression.py</files>
  <action>
Em `backend/regression.py`, atualizar os dados USDA hardcoded para incluir a safra 2024/25 (representada como ano 2025):

1. Atualizar `_USDA_ANNUAL` adicionando o ano 2025. Valores USDA PSD para açúcar global 2024/25 (estimativas publicadas USDA WASDE início de 2025):
   - estoque_inicial: 47.0 (mesmo que estoque_final de 2024)
   - producao: 188.0 (crescimento estimado Brasil + Índia)
   - demanda: 180.5
   - estoque_final: 48.5
   
   ```python
   _USDA_ANNUAL = {
       "year": [2014, 2015, 2016, 2017, 2018, 2019, 2020, 2021, 2022, 2023, 2024, 2025],
       "estoque_inicial": [35.2, 38.1, 39.8, 47.5, 55.4, 53.5, 53.0, 48.4, 45.0, 44.7, 46.5, 47.0],
       "producao":        [178.4, 168.9, 172.6, 185.1, 185.5, 187.3, 176.8, 183.4, 183.8, 185.0, 186.0, 188.0],
       "demanda":         [166.5, 163.7, 165.4, 168.3, 171.4, 174.6, 172.9, 177.5, 179.5, 178.0, 178.5, 180.5],
       "estoque_final":   [38.1, 39.8, 47.5, 55.4, 53.5, 53.0, 48.4, 45.0, 44.7, 46.5, 47.0, 48.5],
   }
   ```

2. Atualizar `_USDA_DEFAULTS` para refletir projeção 2025/26 (próxima safra):
   ```python
   _USDA_DEFAULTS = {
       "estoque_inicial": 48.5,    # estoque_final de 2025
       "producao": 190.0,          # projeção safra 2025/26
       "demanda": 182.0,
       "estoque_final": 48.5,
       "estoque_uso_pct": 26.6,    # estoque_final / demanda * 100 ≈ 26.6
   }
   ```

3. Em `fetch_acucar_history()`: adicionar comentário explicando que o merge `inner` entre yfinance anual e USDA requer que yfinance tenha dados para os mesmos anos. Para 2025, yfinance ainda pode não ter o fechamento anual — isso é esperado e o `inner` join simplesmente excluirá anos sem preço. Não há mudança de código necessária aqui.

4. Verificar que `get_acucar_defaults()` retorna `estoque_uso_pct` de `_USDA_DEFAULTS` — já retorna via `result.update(_USDA_DEFAULTS)`. Nenhuma mudança necessária.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/backend && python -c "from regression import get_acucar_defaults, _USDA_ANNUAL; print('years:', _USDA_ANNUAL['year']); d = get_acucar_defaults(); print('defaults:', {k: v for k, v in d.items() if 'estoque' in k or 'producao' in k or 'demanda' in k})"</automated>
    <manual>Confirmar que _USDA_ANNUAL agora contém 2025 na lista de anos e que _USDA_DEFAULTS.estoque_inicial == 48.5</manual>
  </verify>
  <done>_USDA_ANNUAL contém 12 anos (2014-2025); _USDA_DEFAULTS reflete projeção 2025/26; módulo importa sem erros</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar página Streamlit Regressão Açúcar para dados live</name>
  <files>pages/05_Regressão_Açúcar.py</files>
  <action>
Reescrever `pages/05_Regressão_Açúcar.py` para substituir a dependência de `dadosRegSugar.xlsx` por dados live via yfinance, alinhando com a lógica do `backend/regression.py`. Manter estrutura Streamlit (require_login, show_logo, plotly).

Estrutura do novo arquivo:

```python
import streamlit as st
import numpy as np
import pandas as pd
import plotly.graph_objs as go
import matplotlib.pyplot as plt
import seaborn as sns

from utils import require_login, show_logo

st.set_page_config(page_title="Regressão Açúcar", page_icon="📈", layout="wide")
require_login()
show_logo()

# ── Dados USDA anuais (safras 2014–2025) ──────────────────────────────────────
_USDA_ANNUAL = {
    "year": [2014, 2015, 2016, 2017, 2018, 2019, 2020, 2021, 2022, 2023, 2024, 2025],
    "estoque_inicial": [35.2, 38.1, 39.8, 47.5, 55.4, 53.5, 53.0, 48.4, 45.0, 44.7, 46.5, 47.0],
    "producao":        [178.4, 168.9, 172.6, 185.1, 185.5, 187.3, 176.8, 183.4, 183.8, 185.0, 186.0, 188.0],
    "demanda":         [166.5, 163.7, 165.4, 168.3, 171.4, 174.6, 172.9, 177.5, 179.5, 178.0, 178.5, 180.5],
    "estoque_final":   [38.1, 39.8, 47.5, 55.4, 53.5, 53.0, 48.4, 45.0, 44.7, 46.5, 47.0, 48.5],
}


@st.cache_data(ttl=3600)
def fetch_dados_acucar() -> pd.DataFrame:
    """
    Baixa fechamentos anuais de SB=F, USDBRL=X e CL=F via yfinance (2014-hoje)
    e faz merge inner com dados USDA anuais. Retorna DataFrame indexado por ano.
    """
    import yfinance as yf

    tickers = {"sb_f": "SB=F", "usdbrl": "USDBRL=X", "cl_f": "CL=F"}
    price_data = {}

    for key, ticker in tickers.items():
        try:
            raw = yf.download(ticker, start="2014-01-01", interval="1y",
                              progress=False, auto_adjust=True)
            if raw.empty:
                price_data[key] = pd.Series(dtype=float)
                continue
            close = raw["Close"].dropna()
            close.index = pd.DatetimeIndex(close.index).year
            close = close.groupby(close.index).last()
            price_data[key] = close
        except Exception as e:
            st.warning(f"yfinance {ticker} indisponível: {e}")
            price_data[key] = pd.Series(dtype=float)

    price_df = pd.DataFrame(price_data)

    usda_df = pd.DataFrame(_USDA_ANNUAL).set_index("year")
    usda_df["estoque_uso_pct"] = usda_df["estoque_final"] / usda_df["demanda"] * 100

    merged = price_df.join(usda_df, how="inner").dropna()
    return merged
```

**Seção principal:**

```python
st.title("Previsão do Preço do Açúcar (SB=F)")
st.write("Modelo Ridge/XGBoost treinado em dados anuais USDA + yfinance. Insira as premissas e clique em **Gerar Previsão**.")

# Pré-preencher com valores típicos atuais (USDA defaults 2025/26)
col1, col2, col3 = st.columns(3)
with col1:
    estoque_inicial_proj = st.number_input("Estoque Inicial (Mt)", value=48.5, step=0.5)
    producao_proj = st.number_input("Produção (Mt)", value=190.0, step=1.0)
with col2:
    demanda_proj = st.number_input("Demanda (Mt)", value=182.0, step=1.0)
    estoque_final_proj = st.number_input("Estoque Final (Mt)", value=48.5, step=0.5)
with col3:
    estoque_uso_proj = st.number_input("Estoque/Uso (%)", value=round(48.5 / 182.0 * 100, 1), step=0.5)
    usd_brl_proj = st.number_input("USD/BRL (USDBRL=X)", value=5.85, step=0.05)
    cl_f_proj = st.number_input("Petróleo CL=F (USD/bbl)", value=72.0, step=1.0)

model_type = st.selectbox("Modelo", ["Ridge", "XGBoost"], index=0)

if st.button("Gerar Previsão"):
    from sklearn.linear_model import RidgeCV
    from sklearn.metrics import mean_squared_error, r2_score

    with st.spinner("Buscando dados históricos e treinando modelo..."):
        df = fetch_dados_acucar()

    if len(df) < 6:
        st.error(f"Dados insuficientes: {len(df)} anos após merge (mínimo 6 necessário).")
        st.stop()

    feature_cols = ["estoque_inicial", "producao", "demanda", "estoque_final",
                    "estoque_uso_pct", "usdbrl", "cl_f"]
    y = df["sb_f"].values
    X = df[feature_cols].values

    if model_type == "XGBoost":
        import xgboost as xgb
        model = xgb.XGBRegressor(n_estimators=100, max_depth=3,
                                  learning_rate=0.1, random_state=42, n_jobs=1)
    else:
        model = RidgeCV(alphas=[0.1, 1.0, 10.0, 100.0])

    model.fit(X, y)
    y_pred_train = model.predict(X)
    residuals = y - y_pred_train
    std_res = float(np.std(residuals))
    rmse = float(np.sqrt(mean_squared_error(y, y_pred_train)))
    r2 = float(r2_score(y, y_pred_train))

    # Previsão com inputs do usuário
    input_array = np.array([[estoque_inicial_proj, producao_proj, demanda_proj,
                              estoque_final_proj, estoque_uso_proj,
                              usd_brl_proj, cl_f_proj]])
    sb_f_previsto = float(model.predict(input_array)[0])
    sb_f_min = sb_f_previsto - 1.96 * std_res
    sb_f_max = sb_f_previsto + 1.96 * std_res

    st.success(f"**Preço previsto SB=F: {sb_f_previsto:.2f} ¢/lb**  |  Intervalo 95%: [{sb_f_min:.2f}, {sb_f_max:.2f}]")

    col_m1, col_m2, col_m3 = st.columns(3)
    col_m1.metric("R²", f"{r2:.4f}")
    col_m2.metric("RMSE", f"{rmse:.4f} ¢/lb")
    col_m3.metric("Anos de treino", str(len(df)))

    # Heatmap de correlação
    corr_df = df[["sb_f"] + feature_cols]
    fig_corr, ax = plt.subplots(figsize=(10, 7))
    sns.heatmap(corr_df.corr(), annot=True, cmap="coolwarm", fmt=".2f", linewidths=0.5, ax=ax)
    ax.set_title("Matriz de Correlação — Variáveis do Modelo")
    st.pyplot(fig_corr)
    plt.close(fig_corr)

    # Gráfico histórico real vs previsto
    fig = go.Figure()
    fig.add_trace(go.Scatter(x=list(df.index), y=list(y), mode="lines+markers",
                             name="Real (SB=F)", line=dict(color="blue")))
    fig.add_trace(go.Scatter(x=list(df.index), y=list(y_pred_train), mode="lines+markers",
                             name="Previsto", line=dict(dash="dash", color="orange")))
    fig.update_layout(title="SB=F: Real vs Previsto (treino)", xaxis_title="Ano", yaxis_title="Preço (¢/lb)")
    st.plotly_chart(fig, use_container_width=True)

    # Tabela histórico detalhado
    st.subheader("Histórico de Treino")
    hist_df = pd.DataFrame({
        "Ano": list(df.index),
        "SB=F Real": [round(float(v), 4) for v in y],
        "SB=F Previsto": [round(float(v), 4) for v in y_pred_train],
        "Resíduo": [round(float(r), 4) for r in residuals],
    })
    st.dataframe(hist_df, use_container_width=True)
```

Garantir que o arquivo importa `matplotlib.pyplot as plt` e `seaborn as sns` no topo. Não importar `plotly.subplots as sp` (não usado). Não referenciar `dadosRegSugar.xlsx` em lugar algum. Preservar `from utils import require_login, show_logo`.

Nota sobre unidades: O modelo legacy usava "milhares" (estoque=45000 para 45 Mt). A versão nova usa Mt diretamente (45.0), alinhado com backend/regression.py e com os dados USDA reais.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast; ast.parse(open('pages/05_Regressão_Açúcar.py').read()); print('syntax OK')" && python -c "import subprocess; r = subprocess.run(['grep', '-n', 'dadosRegSugar', 'pages/05_Regressão_Açúcar.py'], capture_output=True, text=True); print('dadosRegSugar references:', r.stdout or 'none — OK')"</automated>
    <manual>Rodar `streamlit run Painel.py` e navegar para Regressão Açúcar. Verificar que a página carrega, inputs mostram valores em Mt (não em milhares), e ao clicar "Gerar Previsão" exibe R², RMSE, preço previsto SB=F com intervalo, heatmap de correlação, gráfico real vs previsto e tabela histórico.</manual>
  </verify>
  <done>pages/05_Regressão_Açúcar.py não referencia dadosRegSugar.xlsx; usa yfinance para preços e _USDA_ANNUAL local; unidades em Mt; modelo exibe preço previsto SB=F com intervalo 95%, R², RMSE, heatmap e gráfico</done>
</task>

</tasks>

<verification>
1. `python -c "from regression import get_acucar_defaults, _USDA_ANNUAL; print(len(_USDA_ANNUAL['year']))"` — deve retornar 12
2. `python -c "import ast; ast.parse(open('pages/05_Regressão_Açúcar.py').read()); print('syntax OK')"` — sintaxe válida
3. Grep confirma ausência de "dadosRegSugar" em pages/05_Regressão_Açúcar.py
4. Grep confirma presença de "yfinance" ou "yf.download" em pages/05_Regressão_Açúcar.py
5. `python -c "from regression import _USDA_DEFAULTS; print(_USDA_DEFAULTS['estoque_inicial'])"` — deve retornar 48.5
</verification>

<success_criteria>
- _USDA_ANNUAL em backend/regression.py inclui ano 2025 (12 entradas)
- _USDA_DEFAULTS reflete projeção 2025/26 (estoque_inicial=48.5, producao=190.0)
- pages/05_Regressão_Açúcar.py não lê dadosRegSugar.xlsx
- Dados de treino vêm de yfinance (SB=F, USDBRL=X, CL=F) + _USDA_ANNUAL local
- Inputs padrão em Mt (48.5, 190.0, etc.) — não em milhares
- Modelo exibe: preço previsto SB=F, intervalo 95%, R², RMSE, heatmap correlação, gráfico, tabela histórico
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-atualizacao-regressoes/19-02-SUMMARY.md`
</output>
