---
phase: 12-feature-pages
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified: []
autonomous: true
requirements: [FEAT-PAGES]

must_haves:
  truths:
    - "frontend/app/app/stress/page.tsx exists and renders stress test scenarios"
    - "frontend/app/app/noticias/page.tsx exists and renders news feed"
    - "frontend/app/app/volatilidade/page.tsx exists and renders volatility chart"
    - "frontend/app/app/admin/page.tsx updated with AdminConfig section for admin users"
    - "ToolGrid in dashboard updated to include links to all new pages"
  artifacts:
    - path: "frontend/app/app/stress/page.tsx"
      provides: "Stress test scenarios page"
    - path: "frontend/app/app/noticias/page.tsx"
      provides: "News feed page"
    - path: "frontend/app/app/volatilidade/page.tsx"
      provides: "Volatility chart page"
---

<objective>
Implement the remaining 3 frontend feature pages (Stress, Notícias, Volatilidade) + update admin page with config section + wire all new pages into ToolGrid on the dashboard.
</objective>

<tasks>

<task>
  <name>Task 1: Stress test page</name>
  <file>frontend/app/app/stress/page.tsx</file>
  <action>create</action>
  <notes>
    - Ticker selector: "Açúcar NY" (SB=F) or "USD/BRL" (USDBRL=X)
    - Fetch GET /api/stress?ticker=SB=F on selection
    - Render shadcn Table with columns: Cenário, Período (start–end), Drawdown (%), Preço Final
    - Color-code drawdown: red for > 20%, yellow for > 10%
    - Title: "Teste de Estresse"
  </notes>
</task>

<task>
  <name>Task 2: Notícias page</name>
  <file>frontend/app/app/noticias/page.tsx</file>
  <action>create</action>
  <notes>
    - Fetch GET /api/news on mount (auto-refresh every 30 min via setInterval)
    - Render list of news cards: title (link), source, published date
    - Use shadcn Card for each item, open link in new tab
    - Show "Última atualização: HH:MM" timestamp
    - Title: "Notícias — Açúcar & Câmbio"
  </notes>
</task>

<task>
  <name>Task 3: Volatilidade page</name>
  <file>frontend/app/app/volatilidade/page.tsx</file>
  <action>create</action>
  <notes>
    - Two tabs: "Açúcar NY" (SB=F) and "USD/BRL" (USDBRL=X)
    - Fetch GET /api/volatility?ticker=SB=F on tab activation
    - Top section: 3 metric cards — Vol 30d, Vol 90d, Vol 1y (annualized %)
    - Bottom section: Recharts LineChart of rolling 30d volatility over time
    - Title: "Volatilidade Realizada"
  </notes>
</task>

<task>
  <name>Task 4: Update admin page with config section</name>
  <file>frontend/app/app/admin/page.tsx</file>
  <action>modify</action>
  <notes>
    - Add second section below SuggestionQueue: "Configurações do Sistema"
    - Fetch GET /api/admin/config
    - Render editable table: key | value (editable input) | description | save button per row
    - Save calls PUT /api/admin/config/{key} with new value
    - Only visible to admin users (already guarded by the page)
  </notes>
</task>

<task>
  <name>Task 5: Update ToolGrid with new pages</name>
  <file>frontend/components/dashboard/ToolGrid.tsx</file>
  <action>modify</action>
  <notes>
    - Add entries for: Focus (/app/focus), VaR (/app/var), Breakeven (/app/breakeven),
      ARIMA (/app/arima), Stress (/app/stress), Notícias (/app/noticias), Volatilidade (/app/volatilidade)
    - Use appropriate icons from lucide-react
    - Group with existing tools or add new "Análise" / "Mercado" category if ToolGrid supports it
  </notes>
</task>

<task>
  <name>Task 6: Commit</name>
  <action>git-commit</action>
  <message>feat(frontend): add Stress, Notícias, Volatilidade pages + admin config + ToolGrid update</message>
</task>

</tasks>

<success_criteria>
- All 3 new pages render without errors
- Admin page shows config table for admin users
- ToolGrid links to all 7 new pages
- News cards open links in new tab
</success_criteria>
