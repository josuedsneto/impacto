---
phase: 12-feature-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [FEAT-PAGES]

must_haves:
  truths:
    - "statsmodels and feedparser added to backend/requirements.txt"
    - "admin_config table SQL migration documented and ready to run"
    - "GET /api/admin/config and PUT /api/admin/config/{key} endpoints exist in main.py"
    - "GET /api/focus, GET /api/var, GET /api/breakeven, GET /api/arima/{ticker}, GET /api/stress, GET /api/news, GET /api/volatility endpoints exist in main.py"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "statsmodels + feedparser added"
    - path: "backend/main.py"
      provides: "All 7 new feature endpoints + 2 admin config endpoints"
    - path: ".planning/phases/12-feature-pages/migrations/admin_config.sql"
      provides: "SQL to create admin_config table with breakeven_fator_conversao seed"
---

<objective>
Add Python dependencies and all FastAPI backend endpoints for the 8 new feature pages + admin config system.

This is the backend-only plan. Frontend pages come in plan 02 and 03.
</objective>

<tasks>

<task>
  <name>Task 1: Add Python dependencies</name>
  <file>backend/requirements.txt</file>
  <action>append</action>
  <content>
statsmodels==0.14.4
feedparser==6.0.11
  </content>
</task>

<task>
  <name>Task 2: Save admin_config migration SQL</name>
  <file>.planning/phases/12-feature-pages/migrations/admin_config.sql</file>
  <action>create</action>
  <content>
```sql
create table if not exists admin_config (
  key text primary key,
  value text not null,
  description text,
  updated_at date
);

insert into admin_config (key, value, description, updated_at)
values ('breakeven_fator_conversao', '1.12045', 'Fator de conversão cents/lb → R$/saca (0.022046 × 50.802)', current_date)
on conflict (key) do nothing;
```
  </content>
  <notes>Run in Supabase Dashboard → SQL Editor before using the breakeven page</notes>
</task>

<task>
  <name>Task 3: Add admin config endpoints to main.py</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/admin/config — requires require_admin, returns all rows ordered by key
    - PUT /api/admin/config/{key} — requires require_admin, upserts key/value/description/updated_at
    - Use SUPABASE_SERVICE_ROLE_KEY (same as other admin routes)
    - AdminConfigUpdateRequest pydantic model: value: str, description: str | None = None
  </notes>
</task>

<task>
  <name>Task 4: Add Focus endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/focus — requires get_current_user
    - Fetch Focus API: https://olinda.bcb.gov.br/olinda/servico/Expectativas/versao/v1/odata/ExpectativaMercadoAnuais?$filter=Indicador eq 'IPCA' and Suavizado eq 'S'&$orderby=Data desc&$top=5&$format=json&$select=Indicador,Data,Mediana,DataReferencia
    - Return list of {indicador, data, mediana, data_referencia}
    - Use httpx or requests (already in requirements)
    - Cache with functools.lru_cache or simple in-memory dict keyed by date (ttl 1h)
  </notes>
</task>

<task>
  <name>Task 5: Add VaR endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/var?ticker=SB=F&confidence=0.95&horizon=1 — requires get_current_user
    - Download 1y of daily closes via yfinance
    - Compute Historical VaR: np.percentile(returns, (1-confidence)*100) * last_price
    - Compute Parametric VaR: norm.ppf(1-confidence) * std * last_price
    - Return {ticker, confidence, horizon, var_historico, var_parametrico, last_price, n_observations}
  </notes>
</task>

<task>
  <name>Task 6: Add Breakeven endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/breakeven — requires get_current_user
    - Read breakeven_fator_conversao from admin_config table (service role)
    - Fetch current SB=F price from yfinance
    - Fetch current USDBRL=X price from yfinance
    - Return {preco_acucar_cents, preco_dolar, fator_conversao, breakeven_reais_saca}
    - breakeven_reais_saca = preco_acucar_cents * fator_conversao * preco_dolar (simplified)
  </notes>
</task>

<task>
  <name>Task 7: Add ARIMA endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/arima/{ticker}?steps=30 — requires get_current_user
    - Download 2y of daily closes via yfinance for the ticker
    - Fit ARIMA(1,1,1) via statsmodels ARIMA
    - Forecast `steps` periods ahead
    - Return {ticker, forecast: [{date, value}], confidence_lower: [...], confidence_upper: [...]}
    - Wrap in try/except — ARIMA can fail on short series; return 400 with detail
  </notes>
</task>

<task>
  <name>Task 8: Add Stress Test endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/stress?ticker=SB=F — requires get_current_user
    - Download full history via yfinance
    - Compute 3 scenarios: worst 5% drawdown period, 2008 crisis analog, COVID crash analog
    - Return {ticker, scenarios: [{name, start, end, drawdown_pct, last_price_after}]}
    - Use simple rolling window max drawdown logic (no external lib needed)
  </notes>
</task>

<task>
  <name>Task 9: Add News endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/news — requires get_current_user
    - Use feedparser to fetch Google News RSS for "açúcar NY futuros" + "dólar real"
    - RSS URL: https://news.google.com/rss/search?q=açúcar+NY+futuros&hl=pt-BR&gl=BR&ceid=BR:pt-419
    - Return top 10 items: [{title, link, published, source}]
    - Simple in-memory cache with timestamp (refresh if > 30min old)
  </notes>
</task>

<task>
  <name>Task 10: Add Volatility endpoint</name>
  <file>backend/main.py</file>
  <action>append</action>
  <notes>
    - GET /api/volatility?ticker=SB=F — requires get_current_user
    - Download 1y daily closes via yfinance
    - Compute: realized_vol_30d, realized_vol_90d, realized_vol_1y (annualized, sqrt(252))
    - Return {ticker, vol_30d, vol_90d, vol_1y, last_price, history: [{date, close, vol_30d_rolling}]}
    - Rolling 30d vol for chart: use pd.Series.rolling(30).std() * sqrt(252)
  </notes>
</task>

<task>
  <name>Task 11: Commit backend</name>
  <action>git-commit</action>
  <message>feat(backend): add 7 feature endpoints + admin config API + statsmodels/feedparser deps</message>
</task>

</tasks>

<success_criteria>
- requirements.txt has statsmodels and feedparser
- main.py has all 9 new endpoints (focus, var, breakeven, arima, stress, news, volatility, admin config x2)
- Each endpoint returns the expected JSON shape
- admin_config.sql migration file exists
</success_criteria>
