---
phase: 12-feature-pages
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: []
autonomous: true
requirements: [FEAT-PAGES]

must_haves:
  truths:
    - "frontend/app/app/focus/page.tsx exists and renders IPCA expectation table"
    - "frontend/app/app/var/page.tsx exists and renders VaR metrics for SB=F and USDBRL=X"
    - "frontend/app/app/breakeven/page.tsx exists and renders breakeven calculation"
    - "frontend/app/app/arima/page.tsx exists and renders ARIMA forecast chart for both tickers"
  artifacts:
    - path: "frontend/app/app/focus/page.tsx"
      provides: "Focus/IPCA expectations page"
    - path: "frontend/app/app/var/page.tsx"
      provides: "Value at Risk page"
    - path: "frontend/app/app/breakeven/page.tsx"
      provides: "Breakeven calculator page"
    - path: "frontend/app/app/arima/page.tsx"
      provides: "ARIMA forecast page"
---

<objective>
Implement the first 4 frontend feature pages: Focus, VaR, Breakeven, ARIMA.

All pages follow the existing pattern: Client Component, fetch with Supabase access token, shadcn/ui layout, Recharts for charts.
</objective>

<context>
Existing pattern (copy from /app/simulation or /app/market):
- 'use client'
- import { createBrowserClient } from '@supabase/ssr'
- const supabase = createBrowserClient(SUPABASE_URL, SUPABASE_ANON_KEY)
- const { data: { session } } = await supabase.auth.getSession()
- fetch(`${process.env.NEXT_PUBLIC_BACKEND_URL}/api/...`, { headers: { Authorization: `Bearer ${session?.access_token}` } })
</context>

<tasks>

<task>
  <name>Task 1: Focus page — IPCA expectation table</name>
  <file>frontend/app/app/focus/page.tsx</file>
  <action>create</action>
  <notes>
    - Fetch GET /api/focus on mount
    - Render shadcn Table with columns: DataReferência (year), Mediana, Data (last update)
    - Title: "Expectativa de Mercado — IPCA (Focus/BCB)"
    - Show loading skeleton while fetching
    - No chart needed — table only
  </notes>
</task>

<task>
  <name>Task 2: VaR page — risk metrics for SB=F and USDBRL=X</name>
  <file>frontend/app/app/var/page.tsx</file>
  <action>create</action>
  <notes>
    - Two tabs: "Açúcar NY" (SB=F) and "USD/BRL" (USDBRL=X)
    - Each tab: fetch GET /api/var?ticker=SB=F&confidence=0.95
    - Show: last price, VaR Histórico (R$ and %), VaR Paramétrico (R$ and %), n_observations
    - Use shadcn Card grid for metrics (same layout as SimulationMetrics)
    - Confidence selector: 90%, 95%, 99% (shadcn Select) — re-fetches on change
    - Title: "Value at Risk"
  </notes>
</task>

<task>
  <name>Task 3: Breakeven page</name>
  <file>frontend/app/app/breakeven/page.tsx</file>
  <action>create</action>
  <notes>
    - Fetch GET /api/breakeven on mount
    - Show: preço açúcar (cents/lb), preço dólar (R$), fator de conversão, breakeven R$/saca
    - Use shadcn Card with large metric display
    - Add simple explanation text: "Preço mínimo de venda para cobrir custos de produção"
    - Title: "Breakeven — Açúcar"
  </notes>
</task>

<task>
  <name>Task 4: ARIMA page — forecast chart</name>
  <file>frontend/app/app/arima/page.tsx</file>
  <action>create</action>
  <notes>
    - Two tabs: "Açúcar NY" (SB=F) and "USD/BRL" (USDBRL=X)
    - Fetch GET /api/arima/{ticker}?steps=30 on tab activation
    - Recharts LineChart: historical last 60 days + forecast 30 days ahead
    - Show confidence interval as shaded area (ComposedChart with Area for CI)
    - steps selector: 15, 30, 60 days (re-fetches on change)
    - Title: "Previsão ARIMA"
    - Note: show warning if ARIMA failed (API returns 400)
  </notes>
</task>

<task>
  <name>Task 5: Commit</name>
  <action>git-commit</action>
  <message>feat(frontend): add Focus, VaR, Breakeven, ARIMA pages</message>
</task>

</tasks>

<success_criteria>
- All 4 pages render without TypeScript errors
- Each page fetches its endpoint with auth header
- Tables and charts render with real data
- Loading states shown during fetch
</success_criteria>
