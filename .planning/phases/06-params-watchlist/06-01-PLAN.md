---
phase: 06-params-watchlist
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
autonomous: true
requirements: [PARAM-01, PARAM-02, PARAM-03]

must_haves:
  truths:
    - "GET /api/params/{ticker} returns the user's saved params or 404 for new tickers"
    - "PUT /api/params/{ticker} upserts volatilidade_custom, taxa_livre_risco, pct_bound_preferido and persists across sessions"
    - "GET /api/watchlist returns all tickers the user has added"
    - "POST /api/watchlist adds a ticker; DELETE /api/watchlist/{ticker} removes it"
    - "All four routes enforce JWT auth — no anonymous access"
  artifacts:
    - path: "backend/main.py"
      provides: "Four new routes: GET/PUT /api/params/{ticker}, GET /api/watchlist, POST /api/watchlist, DELETE /api/watchlist/{ticker}"
      contains: "/api/params"
  key_links:
    - from: "backend/main.py params routes"
      to: "supabase user_parameters table"
      via: "supabase create_client with service role key"
      pattern: "user_parameters.*upsert|user_parameters.*select"
    - from: "backend/main.py watchlist routes"
      to: "supabase watchlist table"
      via: "supabase create_client with service role key"
      pattern: "watchlist.*insert|watchlist.*delete|watchlist.*select"
---

<objective>
Add four FastAPI routes that let authenticated users persist per-ticker simulation parameters and manage a personal watchlist.

Purpose: Provides the API layer that Plans 02 and 03 consume. Without these routes, the frontend cannot read or write params or watchlist data.
Output: Five new route handlers in backend/main.py (GET/PUT params, GET/POST/DELETE watchlist).
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
@supabase/migrations/20260320000005_user_parameters.sql
@supabase/migrations/20260320000006_watchlist.sql
@supabase/migrations/20260320000007_rls_policies.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add params and watchlist routes to backend/main.py</name>
  <files>backend/main.py</files>
  <action>
Add five new routes to backend/main.py after the Options section. Follow the exact pattern of existing routes: Annotated[dict, Depends(get_current_user)], create_client(os.environ["SUPABASE_URL"], os.environ["SUPABASE_SERVICE_ROLE_KEY"]), service role key for all Supabase calls.

Add two Pydantic models near the other models:

```python
class UserParamsRequest(BaseModel):
    volatilidade_custom: float | None = None   # 0–5
    taxa_livre_risco: float | None = None       # -0.5–1
    pct_bound_preferido: float | None = None    # 0.05–2

class WatchlistAddRequest(BaseModel):
    ticker: str
```

Add these five routes:

**GET /api/params/{ticker}**
- Requires get_current_user.
- Query user_parameters WHERE user_id = user["id"] AND ticker = ticker.upper().
- Return 404 with detail "Params not found for ticker" if no row.
- Return the row dict on success.

**PUT /api/params/{ticker}**
- Requires get_current_user.
- ticker = ticker.upper() from path.
- Build update_dict from body: include only non-None fields. If all three are None return 400 "No params provided".
- Add updated_at = date.today().isoformat() to update_dict (use datetime import already present).
- Upsert into user_parameters: on_conflict="user_id,ticker". Payload includes user_id, ticker, plus update_dict fields.
- Return {"ticker": ticker, "saved": True}.

**GET /api/watchlist**
- Requires get_current_user.
- Query watchlist WHERE user_id = user["id"], ORDER BY created_at ASC.
- Return {"tickers": [row["ticker"] for row in result.data]}.

**POST /api/watchlist**
- Requires get_current_user.
- ticker = body.ticker.strip().upper(). Return 400 if empty.
- Insert into watchlist: {user_id: user["id"], ticker: ticker}. ON CONFLICT DO NOTHING via upsert with ignore_duplicates=True, on_conflict="user_id,ticker".
- Return {"ticker": ticker, "added": True}.

**DELETE /api/watchlist/{ticker}**
- Requires get_current_user.
- Delete from watchlist WHERE user_id = user["id"] AND ticker = ticker.upper().
- Return {"ticker": ticker, "removed": True}.

Note: updated_at needs `from datetime import date as date_type` already imported — use `from datetime import datetime` or just pass `datetime.utcnow().isoformat()`. Check existing imports: `from datetime import date as date_type` is already imported. Add `from datetime import datetime` if not present, or use `date.today().isoformat()` for updated_at (acceptable — it's a date not timestamp). Do not break existing imports.

Do NOT add RLS-bypass logic — the service role key bypasses RLS automatically but we still filter by user_id at the query level (matches the SIM-04 pattern already used for simulations).
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast; ast.parse(open('backend/main.py').read()); print('syntax ok')" && python -c "import subprocess; src=open('backend/main.py').read(); checks=['/api/params/{ticker}','/api/watchlist','UserParamsRequest','WatchlistAddRequest']; [print(f'FOUND: {c}') if c in src else print(f'MISSING: {c}') for c in checks]"</automated>
    <manual>Verify five new route definitions appear in backend/main.py and the file has no syntax errors.</manual>
  </verify>
  <done>backend/main.py parses without errors, contains GET/PUT /api/params/{ticker} and GET/POST/DELETE /api/watchlist routes, all using get_current_user dependency and service role Supabase client.</done>
</task>

</tasks>

<verification>
Run: `python -c "import ast; ast.parse(open('backend/main.py').read()); print('syntax ok')"`
Run: `grep -n "api/params\|api/watchlist" backend/main.py` — should show 5 route decorators.
</verification>

<success_criteria>
Five new routes exist in backend/main.py: GET /api/params/{ticker}, PUT /api/params/{ticker}, GET /api/watchlist, POST /api/watchlist, DELETE /api/watchlist/{ticker}. All require JWT. File parses without syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-params-watchlist/06-01-SUMMARY.md`
</output>
