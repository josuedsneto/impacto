---
phase: 17-simulation-history-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/09_Monte_Carlo.py
autonomous: true
requirements:
  - MC-01

must_haves:
  truths:
    - "Simulação GBM usa drift log-correto (mu - 0.5*sigma^2), não a média aritmética bruta dos retornos"
    - "Métricas exibidas após simulação são P5, P50, P95 — consistentes com o fan chart"
    - "PCT_BOUND=0.50 permanece inalterado e documentado com comentário explicativo"
    - "num_simulacoes=10000 permanece inalterado"
    - "Código passa em leitura estática sem erros de importação"
  artifacts:
    - path: "pages/09_Monte_Carlo.py"
      provides: "Monte Carlo com drift GBM correto e métricas consistentes"
      contains: "mu - 0.5 * sigma**2"
  key_links:
    - from: "simulacao_monte_carlo()"
      to: "np.random.normal(drift, std, ...)"
      via: "drift = media - 0.5 * std**2"
      pattern: "drift.*0\\.5.*std"
    - from: "percentis exibidos"
      to: "fan chart"
      via: "p5, p50, p95 métricas"
      pattern: "percentil_5.*percentil_50.*percentil_95"
---

<objective>
Corrigir o cálculo do drift no Monte Carlo para ser matematicamente correto para GBM (Geometric Brownian Motion), e alinhar as métricas exibidas abaixo do gráfico com os percentis já calculados no fan chart.

Purpose: A média aritmética dos retornos diários (`mu`) não é o drift correto para GBM log-normal. O drift correto no espaço log é `mu - 0.5*sigma^2`. Sem essa correção, a mediana simulada desvia sistematicamente do preço esperado. Os requisitos do cliente (MC-01) pedem revisão e correção da implementação.

Output: pages/09_Monte_Carlo.py com drift corrigido, métricas P5/P50/P95 e comentários explicativos.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/09_Monte_Carlo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir drift GBM e alinhar métricas com fan chart</name>
  <files>pages/09_Monte_Carlo.py</files>
  <action>
Editar pages/09_Monte_Carlo.py com as seguintes mudanças cirúrgicas:

**1. Corrigir drift na função simulacao_monte_carlo():**

Alterar a assinatura para receber `drift` em vez de `media`, e usar `drift` diretamente no `np.random.normal`. A função em si não calcula o drift — o chamador passa o valor correto.

```python
def simulacao_monte_carlo(preco_inicial, drift, std, dias_simulados, num_simulacoes, limite_inferior, limite_superior):
    # drift = mu - 0.5*sigma^2 (correção log-normal para GBM não tendencioso)
    retornos = np.random.normal(drift, std, (dias_simulados, num_simulacoes))
    fator = np.cumprod(1 + retornos, axis=0)
    precos_simulados = np.clip(preco_inicial * fator, limite_inferior, limite_superior)
    return precos_simulados
```

**2. Calcular drift correto no call site (antes do botão Simular):**

Após calcular `media_retornos_diarios` e `desvio_padrao_retornos_diarios`, adicionar:

```python
# Correção log-normal: drift GBM = mu - 0.5*sigma^2
# Sem essa correção, a mediana simulada desvia sistematicamente do preço atual
drift_gbm = media_retornos_diarios - 0.5 * desvio_padrao_retornos_diarios ** 2
```

**3. Atualizar chamada da função:**

Substituir:
```python
simulacoes = simulacao_monte_carlo(valor_simulado, media_retornos_diarios, desvio_padrao_retornos_diarios, ...)
```
Por:
```python
simulacoes = simulacao_monte_carlo(valor_simulado, drift_gbm, desvio_padrao_retornos_diarios, ...)
```

**4. Substituir métricas P20/P80 por P5/P50/P95 (consistente com fan chart):**

Dentro do bloco `if st.button("Simular"):`, substituir:
```python
percentil_20 = np.percentile(simulacoes[-1], 20)
percentil_80 = np.percentile(simulacoes[-1], 80)
prob_acima_valor = np.mean(simulacoes[-1] > valor_simulado) * 100
prob_abaixo_valor = np.mean(simulacoes[-1] < valor_simulado) * 100
```
Por:
```python
percentil_5  = np.percentile(simulacoes[-1], 5)
percentil_50 = np.percentile(simulacoes[-1], 50)
percentil_95 = np.percentile(simulacoes[-1], 95)
prob_acima_valor  = np.mean(simulacoes[-1] > valor_simulado) * 100
prob_abaixo_valor = np.mean(simulacoes[-1] < valor_simulado) * 100
```

**5. Atualizar exibição das métricas (4 colunas):**

Substituir:
```python
col1, col2, col3, col4 = st.columns(4)
col1.metric("P20", f"{percentil_20:.2f}")
col2.metric("P80", f"{percentil_80:.2f}")
col3.metric(f"Prob. acima de {valor_simulado:.2f}", f"{prob_acima_valor:.1f}%")
col4.metric(f"Prob. abaixo de {valor_simulado:.2f}", f"{prob_abaixo_valor:.1f}%")
```
Por:
```python
col1, col2, col3, col4, col5 = st.columns(5)
col1.metric("P5",  f"{percentil_5:.2f}")
col2.metric("P50 (Mediana)", f"{percentil_50:.2f}")
col3.metric("P95", f"{percentil_95:.2f}")
col4.metric(f"Prob. acima de {valor_simulado:.2f}", f"{prob_acima_valor:.1f}%")
col5.metric(f"Prob. abaixo de {valor_simulado:.2f}", f"{prob_abaixo_valor:.1f}%")
```

**Manter inalterado:**
- PCT_BOUND=0.50 (decisão arquitetural confirmada)
- num_simulacoes=10000
- Fan chart (p5/p25/p50/p75/p95 traces)
- Histograma
- @st.cache_data na função baixar_dados_mc
- Lógica de bounds (limite_inferior, limite_superior)
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast, sys; ast.parse(open('pages/09_Monte_Carlo.py').read()); print('AST OK')"</automated>
    <manual>Verificar visualmente que o arquivo contém: (1) "mu - 0.5 * sigma" ou equivalente no comentário/código, (2) "percentil_5" e "percentil_95" nas métricas, (3) chamada da simulação usa drift_gbm</manual>
  </verify>
  <done>
    - pages/09_Monte_Carlo.py contém `drift_gbm = media_retornos_diarios - 0.5 * desvio_padrao_retornos_diarios ** 2`
    - Função simulacao_monte_carlo() usa parâmetro `drift` (não `media`)
    - Métricas exibidas são P5, P50 (Mediana), P95 — alinhadas com o fan chart
    - Arquivo passa parse AST sem erros
  </done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/netin/OneDrive/Documentos/Code/impacto
python -c "
import ast
src = open('pages/09_Monte_Carlo.py').read()
ast.parse(src)
assert 'drift_gbm' in src, 'drift_gbm ausente'
assert '0.5 * desvio_padrao_retornos_diarios ** 2' in src or '0.5*desvio_padrao' in src, 'correcao GBM ausente'
assert 'percentil_5' in src, 'P5 ausente'
assert 'percentil_95' in src, 'P95 ausente'
assert 'percentil_50' in src, 'P50 ausente'
print('PASS: todas as verificacoes OK')
"
```
</verification>

<success_criteria>
- Drift corrigido para GBM log-normal (mu - 0.5*sigma^2)
- Métricas P5/P50/P95 exibidas (consistentes com fan chart)
- PCT_BOUND e num_simulacoes inalterados
- Sem erros de sintaxe Python
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-simulation-history-page/17-01-SUMMARY.md`
</output>
