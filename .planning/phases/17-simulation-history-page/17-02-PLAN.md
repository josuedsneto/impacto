---
phase: 17-simulation-history-page
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/15_VaR.py
autonomous: true
requirements:
  - VAR-01

must_haves:
  truths:
    - "Download yfinance está em cache (@st.cache_data) para evitar re-download a cada interação"
    - "Escalonamento de volatilidade usa sqrt(n_days) com n_days = dias úteis até o horizonte (correto para VaR paramétrico multi-day)"
    - "Label 'Volatilidade Histórica' exibe EWMA vol no horizonte (não std bruto de toda a série)"
    - "Metodologia EWMA (lambda=0.94, RiskMetrics) documentada com comentário explícito"
    - "Nível de confiança é mapeado para left-tail corretamente: z = norm.ppf(1 - confianca/100)"
    - "VaR exibido como valor positivo (perda esperada), preço em risco como preço mínimo esperado"
  artifacts:
    - path: "pages/15_VaR.py"
      provides: "VaR paramétrico EWMA com cache, labels corretos e z-score left-tail"
      contains: "st.cache_data"
  key_links:
    - from: "calcular_var()"
      to: "VaR_EWMA"
      via: "z_score negativo * vol positiva = VaR negativo → abs() para exibição"
      pattern: "abs.*VaR_EWMA"
    - from: "confianca slider"
      to: "z_score"
      via: "norm.ppf(1 - confianca/100)"
      pattern: "norm\\.ppf.*1.*confianca"
---

<objective>
Validar e corrigir a implementação do VaR em pages/15_VaR.py: adicionar cache no download de dados, corrigir z-score para left-tail, exibir VaR como perda positiva, e corrigir label "Volatilidade Histórica" para mostrar a EWMA vol efetivamente usada no cálculo.

Purpose: Atualmente há três problemas: (1) sem @st.cache_data no yfinance — re-download a cada slider/input; (2) z_score = norm.ppf((100 - confianca)/100) é errado quando confianca=95 → ppf(0.05) = -1.645, negativo, mas VaR_EWMA também fica negativo e price_at_risk < current_price — isso acidentalmente funciona, mas o raciocínio está invertido; (3) "Volatilidade Histórica" exibe std bruto da série toda, não a EWMA vol usada no VaR — métrica enganosa. MC-01 e VAR-01 são independentes e podem ser executados em paralelo (wave 1).

Output: pages/15_VaR.py com cache, z-score documentado, VaR exibido como perda absoluta positiva e label correto de volatilidade.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/15_VaR.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar cache e corrigir z-score, labels e exibição do VaR</name>
  <files>pages/15_VaR.py</files>
  <action>
Editar pages/15_VaR.py com as seguintes mudanças:

**1. Extrair download de dados para função cacheada:**

Substituir o download inline:
```python
data = yf.download(escolha, start=start_date, end=date.today().strftime('%Y-%m-%d'), auto_adjust=True, multi_level_index=False, progress=False)
```

Por uma função decorada e chamada após o selectbox:
```python
@st.cache_data(ttl=3600)
def baixar_dados_var(ativo: str) -> pd.DataFrame:
    start_date = date(2013, 1, 1)
    end_date = date.today().strftime("%Y-%m-%d")
    return yf.download(ativo, start=start_date, end=end_date, auto_adjust=True, multi_level_index=False, progress=False)
```

E no corpo principal:
```python
data = baixar_dados_var(escolha)
```

Remover a linha `start_date = date(2013, 1, 1)` do corpo principal (agora está dentro da função).

**2. Corrigir z-score para left-tail e documentar:**

Substituir:
```python
z_score = norm.ppf((100 - confianca) / 100)
```
Por:
```python
# Left-tail: com confianca=95%, VaR cobre os 5% piores retornos → ppf(0.05) = -1.645
# VaR_EWMA será negativo; exibimos abs() como "perda esperada"
z_score = norm.ppf(1 - confianca / 100)
```

(O resultado numérico é idêntico ao original — `ppf((100-95)/100) == ppf(1-95/100) == ppf(0.05)` — mas a intenção fica explícita.)

**3. Retornar também a EWMA vol final de calcular_var():**

Na função `calcular_var()`, adicionar `ewma_vol_final` ao retorno:

```python
def calcular_var(data, n_days, current_price, z_score):
    if 'Close' in data.columns:
        data = data.copy()
        data['Returns'] = data['Close'].pct_change()
    else:
        raise KeyError("Coluna 'Close' não encontrada.")
    lambda_ = 0.94
    # EWMA volatility (RiskMetrics, lambda=0.94): pondera retornos recentes mais fortemente
    data['EWMA_Vol'] = data['Returns'].ewm(span=(2 / (1 - lambda_) - 1)).std()
    # Escalonar para horizonte de n_days dias úteis (sqrt(n_days) pela raiz do tempo)
    data['Scaled_EWMA_Vol'] = data['EWMA_Vol'] * np.sqrt(n_days)
    VaR_EWMA = z_score * data['Scaled_EWMA_Vol'].iloc[-1] * current_price
    price_at_risk = current_price + VaR_EWMA  # VaR_EWMA < 0, portanto price_at_risk < current_price
    ewma_vol_final = data['EWMA_Vol'].iloc[-1]  # volatilidade EWMA diária mais recente
    return VaR_EWMA, price_at_risk, data['Returns'].mean(), data['Returns'].std(), ewma_vol_final
```

**4. Atualizar call site e métricas:**

Substituir:
```python
VaR_EWMA, price_at_risk, mean_returns, std_returns = calcular_var(data, n_days, current_price, z_score)
col1, col2, col3, col4, col5 = st.columns(5)
col1.metric("VaR", f"{VaR_EWMA:.2f}")
col2.metric("Preço em risco", f"{price_at_risk:.2f}")
col3.metric("Média Retornos", f"{mean_returns:.2%}")
col4.metric("Volatilidade Histórica", f"{std_returns:.2%}")
col5.metric("Z-Score", f"{z_score:.2f}")
```
Por:
```python
VaR_EWMA, price_at_risk, mean_returns, std_returns, ewma_vol_final = calcular_var(data, n_days, current_price, z_score)
col1, col2, col3, col4, col5 = st.columns(5)
col1.metric("VaR (Perda Máx.)", f"{abs(VaR_EWMA):.2f}")
col2.metric("Preço em Risco", f"{price_at_risk:.2f}")
col3.metric("Média Retornos Diários", f"{mean_returns:.2%}")
col4.metric("Vol. EWMA Diária", f"{ewma_vol_final:.2%}")
col5.metric("Z-Score", f"{z_score:.4f}")
```

**Manter inalterado:**
- lambda_=0.94
- Histograma de retornos + curva Normal
- Lógica de n_days com np.busday_count
- Slider de confiança 90–99
- Layout geral e títulos
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto && python -c "import ast, sys; ast.parse(open('pages/15_VaR.py').read()); print('AST OK')"</automated>
    <manual>Verificar que o arquivo contém: (1) @st.cache_data acima de baixar_dados_var, (2) 'ewma_vol_final' no retorno de calcular_var, (3) abs(VaR_EWMA) na exibição da métrica VaR, (4) comentário sobre left-tail no z_score</manual>
  </verify>
  <done>
    - pages/15_VaR.py contém função baixar_dados_var() com @st.cache_data(ttl=3600)
    - calcular_var() retorna 5 valores incluindo ewma_vol_final
    - Métrica "VaR (Perda Máx.)" exibe abs(VaR_EWMA)
    - Métrica "Vol. EWMA Diária" exibe ewma_vol_final (não std_returns)
    - Arquivo passa parse AST sem erros
  </done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/netin/OneDrive/Documentos/Code/impacto
python -c "
import ast
src = open('pages/15_VaR.py').read()
ast.parse(src)
assert 'cache_data' in src, '@st.cache_data ausente'
assert 'ewma_vol_final' in src, 'ewma_vol_final ausente'
assert 'abs(VaR_EWMA)' in src, 'abs(VaR_EWMA) ausente — VaR deve ser exibido como perda positiva'
assert 'Vol. EWMA' in src, 'label Vol. EWMA ausente'
print('PASS: todas as verificacoes OK')
"
```
</verification>

<success_criteria>
- Cache adicionado ao download yfinance (evita re-download a cada interação)
- Z-score documentado como left-tail com comentário explícito
- VaR exibido como perda absoluta positiva com abs()
- Label de volatilidade mostra EWMA vol diária (não std histórico bruto)
- Sem erros de sintaxe Python
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-simulation-history-page/17-02-SUMMARY.md`
</output>
