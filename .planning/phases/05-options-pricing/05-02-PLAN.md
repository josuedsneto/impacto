---
phase: 05-options-pricing
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - frontend/components/options/PayoffBuilder.tsx
  - frontend/components/options/PayoffChart.tsx
  - frontend/components/options/BSPricer.tsx
  - frontend/components/options/MCPricer.tsx
  - frontend/app/app/options/page.tsx
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03]

must_haves:
  truths:
    - "User can add two or more option legs and the payoff diagram updates to reflect the combined strategy"
    - "User changes the volatility input and the Black-Scholes price recalculates immediately"
    - "MC pricer calls /api/options/mc-price and displays a price consistent with BS at ATM"
  artifacts:
    - path: "frontend/components/options/PayoffBuilder.tsx"
      provides: "Add/remove legs form + fetch to /api/options/payoff"
      min_lines: 60
    - path: "frontend/components/options/PayoffChart.tsx"
      provides: "Recharts LineChart rendering payoff series"
    - path: "frontend/components/options/BSPricer.tsx"
      provides: "Controlled form for S, K, T, r, sigma — recalculates on every input change"
    - path: "frontend/components/options/MCPricer.tsx"
      provides: "Form + fetch to /api/options/mc-price, shows MC price"
    - path: "frontend/app/app/options/page.tsx"
      provides: "Options page assembling all four components in tabs"
  key_links:
    - from: "frontend/app/app/options/page.tsx"
      to: "frontend/components/options/PayoffBuilder.tsx"
      via: "import PayoffBuilder"
      pattern: "import PayoffBuilder"
    - from: "frontend/components/options/PayoffBuilder.tsx"
      to: "/api/options/payoff"
      via: "fetch with Bearer token"
      pattern: "fetch.*options/payoff"
    - from: "frontend/components/options/BSPricer.tsx"
      to: "/api/options/bs-price"
      via: "fetch on every input change (debounced 300ms)"
      pattern: "fetch.*options/bs-price"
---

<objective>
Next.js `/app/options` page with four React components: multi-leg payoff diagram builder, payoff Recharts chart, Black-Scholes pricer, and risk-neutral MC pricer.

Purpose: Deliver OPT-01/02/03 to users as an interactive options analysis tool, following the same patterns as the simulation page.
Output: 4 new components + 1 new page.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/app/app/simulation/page.tsx
@frontend/components/simulation/FanChart.tsx
@frontend/components/simulation/SimulationForm.tsx
@.planning/phases/05-options-pricing/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PayoffBuilder, PayoffChart, BSPricer, MCPricer components</name>
  <files>
    frontend/components/options/PayoffBuilder.tsx
    frontend/components/options/PayoffChart.tsx
    frontend/components/options/BSPricer.tsx
    frontend/components/options/MCPricer.tsx
  </files>
  <action>
Create `frontend/components/options/` directory with four components. Follow the same patterns as `frontend/components/simulation/` — "use client", createBrowserClient for token, Tailwind-only styling, no shadcn Card dependency.

Before writing any code, read `node_modules/next/dist/docs/` for any breaking changes relevant to "use client" components and fetch patterns, as per frontend/AGENTS.md.

**PayoffBuilder.tsx**
- "use client" component
- Props: `onPayoffResult: (result: {prices: number[], payoff: number[]}) => void`
- State: `legs` array of `{id: string, type: 'call'|'put', strike: number, premium: number, position: 'long'|'short', quantity: number}`
- UI: "Adicionar Leg" button adds a new leg with defaults (type=call, strike=20, premium=1, position=long, quantity=1). Each leg has a remove button and inline inputs for all fields. A "Calcular Payoff" button at the bottom.
- On submit: fetch Bearer token via `createBrowserClient`, POST to `${API}/api/options/payoff` with `{legs}`, call `onPayoffResult(data)` on success. Show red error paragraph on failure.
- `const API = process.env.NEXT_PUBLIC_API_URL ?? ""`

**PayoffChart.tsx**
- "use client" component
- Props: `prices: number[], payoff: number[]`
- Transform into Recharts data: `prices.map((p, i) => ({ preco: p.toFixed(2), payoff: payoff[i] }))`
- Render `ResponsiveContainer (100% x 350px)` with `LineChart` (not AreaChart — payoff can be negative)
- One `Line` dataKey="payoff" stroke="#1d4ed8" dot={false} isAnimationActive={false}
- `XAxis dataKey="preco"` with label "Preço do Ativo"; `YAxis` with label "P&L"
- Add `ReferenceLine y={0} stroke="#94a3b8" strokeDasharray="4 2"` to show break-even
- Import ReferenceLine from recharts (already installed as recharts@2.15.4)

**BSPricer.tsx**
- "use client" component
- State: `S`, `K`, `T`, `r`, `sigma` (all numbers with defaults: S=20, K=20, T=1, r=0.05, sigma=0.20)
- State: `price: number | null`, `loading: boolean`, `error: string | null`
- On every input change: call `handleCalculate()` (debounced 300ms using `useRef` + `setTimeout`/`clearTimeout` pattern — do NOT use any debounce library)
- `handleCalculate`: fetch Bearer token, POST to `${API}/api/options/bs-price` with {S, K, T, r, sigma}, set `price` from `data.price`
- UI: 5 labeled inputs in a grid (2 cols), result shown as large bold number: "Preço BS: {price?.toFixed(4)}" or "—" while loading
- Use `@/components/ui/input` and `@/components/ui/label` (already exist)

**MCPricer.tsx**
- "use client" component
- State: `S`, `K`, `T`, `r`, `sigma`, `num_simulacoes` (defaults: S=20, K=20, T=1, r=0.05, sigma=0.20, num_simulacoes=10000)
- State: `price: number | null`, `loading: boolean`, `error: string | null`
- Manual submit only (no auto-recalculate — MC is slow). "Calcular (MC)" button.
- On submit: fetch Bearer token, POST to `${API}/api/options/mc-price` with all fields, set `price`
- UI: same 5+1 inputs as BSPricer plus num_simulacoes, "Calcular (MC)" button, show result "Preço MC: {price?.toFixed(4)}"
- Show loading state "Calculando..." on button while `loading === true`
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "options/" | head -20 || echo "No options/ errors"</automated>
    <manual>Confirm all four files exist in frontend/components/options/</manual>
  </verify>
  <done>All four component files exist, no TypeScript errors in options/ components, recharts LineChart + ReferenceLine used in PayoffChart.</done>
</task>

<task type="auto">
  <name>Task 2: Create /app/options page assembling all components</name>
  <files>frontend/app/app/options/page.tsx</files>
  <action>
Create `frontend/app/app/options/page.tsx` following the exact same structure as `frontend/app/app/simulation/page.tsx`.

Before writing any code, read `node_modules/next/dist/docs/` for breaking changes relevant to page.tsx in App Router, as per frontend/AGENTS.md.

- "use client" page
- State: `payoffResult: {prices: number[], payoff: number[]} | null`
- Import and use `Tabs, TabsList, TabsTrigger, TabsContent` from `@/components/ui/tabs` (already exists)
- Three tabs: "Payoff", "Black-Scholes", "MC Pricer"

**Tab "payoff":**
- Render `<PayoffBuilder onPayoffResult={setPayoffResult} />`
- Below it, conditionally render `<PayoffChart prices={payoffResult.prices} payoff={payoffResult.payoff} />` when `payoffResult !== null`

**Tab "black-scholes":**
- Render `<BSPricer />`
- Add a small note below: `<p className="text-sm text-muted-foreground mt-4">OPT-02: volatilidade customizável. Recalcula automaticamente a cada mudança.</p>`

**Tab "mc-pricer":**
- Render `<MCPricer />`
- Add note: `<p className="text-sm text-muted-foreground mt-4">OPT-03: drift risk-neutral (r − 0.5σ²). Consistente com Black-Scholes no ATM.</p>`

Page header: `<h1 className="text-2xl font-semibold">Opções e Pricing</h1>`
Outer div: `className="container mx-auto py-8 space-y-6"`

Do NOT add navigation links — the nav is handled by the existing app layout.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "options" | head -20 || echo "No options errors" && test -f app/app/options/page.tsx && echo "page.tsx exists"</automated>
  </verify>
  <done>frontend/app/app/options/page.tsx exists, no TypeScript errors, three tabs rendering all four components.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/netin/OneDrive/Documentos/Code/impacto/frontend
# TypeScript check across all options files
npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "error" | head -30
# Confirm files exist
ls components/options/
ls app/app/options/
```
Expected: No TS errors in options components. All 5 files present.
</verification>

<success_criteria>
- frontend/components/options/PayoffBuilder.tsx: add/remove legs, POST to /api/options/payoff, calls onPayoffResult
- frontend/components/options/PayoffChart.tsx: Recharts LineChart with break-even ReferenceLine at y=0
- frontend/components/options/BSPricer.tsx: auto-recalculates BS price on every input change (300ms debounce)
- frontend/components/options/MCPricer.tsx: manual submit, calls /api/options/mc-price
- frontend/app/app/options/page.tsx: three-tab layout assembling all components
- No TypeScript errors in any options file
</success_criteria>

<output>
After completion, create `.planning/phases/05-options-pricing/05-02-SUMMARY.md`
</output>
