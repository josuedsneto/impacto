---
phase: 18-fixacoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/10_Fixações.py
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04

must_haves:
  truths:
    - "A página aparece no nav do Streamlit como 'Fixações' (não 'Mercado')"
    - "Ao selecionar Estocástico Lento, o usuário pode configurar períodos %K e %D via sidebar"
    - "Ao selecionar RSI, o usuário pode configurar o período via sidebar"
    - "Ao selecionar Bandas de Bollinger, o usuário pode configurar janela e número de desvios padrão via sidebar"
    - "Os indicadores calculam e exibem gráficos corretamente com os parâmetros configurados"
  artifacts:
    - path: "pages/10_Fixações.py"
      provides: "Página Fixações com indicadores técnicos configuráveis"
      exports: []
  key_links:
    - from: "st.sidebar inputs (período %K, %D)"
      to: "calcular_estocastico_lento(data, window=periodo_k, smooth_k=periodo_d)"
      via: "variáveis locais passadas às funções de cálculo"
      pattern: "periodo_k.*smooth_k"
    - from: "st.sidebar inputs (período RSI)"
      to: "calcular_RSI(data, window=periodo_rsi)"
      via: "variável local passada à função"
      pattern: "periodo_rsi"
    - from: "st.sidebar inputs (janela BB, desvios)"
      to: "calcular_bollinger_bands(data, window=janela_bb, num_std_dev=desvios)"
      via: "variáveis locais passadas à função"
      pattern: "janela_bb.*desvios"
---

<objective>
Renomear a página "Mercado" para "Fixações" e expor parâmetros configuráveis via sidebar para os indicadores Estocástico Lento, RSI e Bandas de Bollinger.

Purpose: O cliente quer a página com nome "Fixações" e quer controlar os parâmetros dos indicadores técnicos sem editar código.
Output: `pages/10_Fixações.py` — página funcional com nome atualizado e inputs configuráveis por indicador.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renomear arquivo e atualizar título da página</name>
  <files>pages/10_Fixações.py</files>
  <action>
Copiar o conteúdo de `pages/10_Mercado.py` para `pages/10_Fixações.py` e então deletar `pages/10_Mercado.py`.

No novo arquivo `pages/10_Fixações.py`, fazer as seguintes alterações:
1. Linha `st.set_page_config(...)`: mudar `page_title="Mercado"` para `page_title="Fixações"` e `page_icon="📈"` mantido.
2. Linha `st.title("Mercado")`: mudar para `st.title("Fixações")`.

Nenhuma outra alteração de lógica neste task — apenas rename + título.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pages/10_Fixações.py').read()); print('syntax OK')"</automated>
    <manual>Verificar que o arquivo `pages/10_Mercado.py` não existe mais e que `pages/10_Fixações.py` existe com os títulos corretos.</manual>
  </verify>
  <done>Arquivo `pages/10_Mercado.py` removido. Arquivo `pages/10_Fixações.py` existe com `page_title="Fixações"` e `st.title("Fixações")`.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar parâmetros configuráveis para Estocástico Lento, RSI e Bollinger Bands</name>
  <files>pages/10_Fixações.py</files>
  <action>
Em `pages/10_Fixações.py`, adicionar inputs configuráveis usando `st.sidebar` com seção condicional: os inputs de parâmetros aparecem somente quando o indicador correspondente está selecionado.

Estrutura a implementar logo após a linha `indicador_selecionado = st.selectbox(...)`:

```python
# Parâmetros configuráveis por indicador
if indicador_selecionado == "Estocástico Lento":
    with st.sidebar:
        st.subheader("Parâmetros — Estocástico Lento")
        periodo_k = st.number_input("Período %K", min_value=1, max_value=100, value=14, step=1)
        periodo_d = st.number_input("Período %D (suavização)", min_value=1, max_value=20, value=3, step=1)
elif indicador_selecionado == "RSI":
    with st.sidebar:
        st.subheader("Parâmetros — RSI")
        periodo_rsi = st.number_input("Período RSI", min_value=1, max_value=100, value=14, step=1)
elif indicador_selecionado == "Bandas de Bollinger":
    with st.sidebar:
        st.subheader("Parâmetros — Bandas de Bollinger")
        janela_bb = st.number_input("Janela (períodos)", min_value=2, max_value=200, value=20, step=1)
        desvios_bb = st.number_input("Desvios padrão", min_value=0.5, max_value=5.0, value=2.0, step=0.5)
```

Nos blocos `if st.button("Calcular"):`, substituir as chamadas hardcoded pelos parâmetros das variáveis acima:

1. No bloco `elif indicador_selecionado == "Estocástico":`:
   - Mudar o label do selectbox de `"Estocástico"` para `"Estocástico Lento"` (na linha do `st.selectbox` de indicadores).
   - Mudar a chave do bloco elif de `"Estocástico"` para `"Estocástico Lento"`.
   - Substituir `calcular_estocastico_lento(data_filtrado)` por `calcular_estocastico_lento(data_filtrado, window=periodo_k, smooth_k=periodo_d)`.
   - Mudar o nome da coluna de `'Estocástico'` para `'Estocástico Lento'` na atribuição e no `name=` do trace do Plotly.

2. No bloco `elif indicador_selecionado == "RSI":`:
   - Substituir `calcular_RSI(data_filtrado)` por `calcular_RSI(data_filtrado, window=periodo_rsi)`.

3. No bloco `elif indicador_selecionado == "Bandas de Bollinger":`:
   - Substituir `calcular_bollinger_bands(data_filtrado)` por `calcular_bollinger_bands(data_filtrado, window=janela_bb, num_std_dev=desvios_bb)`.

Não alterar nenhuma outra lógica (EWMA, CCI, MACD permanecem sem inputs configuráveis pois não são requisitos deste phase).

IMPORTANTE: As variáveis `periodo_k`, `periodo_d`, `periodo_rsi`, `janela_bb`, `desvios_bb` são definidas ANTES do botão "Calcular" e ficam disponíveis dentro do bloco `if st.button("Calcular"):` por escopo normal do Python/Streamlit.
  </action>
  <verify>
    <automated>python -c "
import ast
src = open('pages/10_Fixações.py').read()
ast.parse(src)
assert 'periodo_k' in src, 'periodo_k not found'
assert 'periodo_d' in src, 'periodo_d not found'
assert 'periodo_rsi' in src, 'periodo_rsi not found'
assert 'janela_bb' in src, 'janela_bb not found'
assert 'desvios_bb' in src, 'desvios_bb not found'
assert 'Estocástico Lento' in src, 'label Estocástico Lento not found'
print('all checks passed')
"</automated>
    <manual>Abrir a app (`streamlit run Painel.py`), navegar para Fixações, selecionar "Estocástico Lento" e verificar que a sidebar exibe "Período %K" e "Período %D". Repetir para RSI e Bandas de Bollinger. Clicar em "Calcular" e confirmar que o gráfico reflete os parâmetros inseridos.</manual>
  </verify>
  <done>
- Sidebar exibe inputs condicionais por indicador selecionado.
- Estocástico Lento: inputs %K e %D presentes, valores passados para `calcular_estocastico_lento()`.
- RSI: input de período presente, valor passado para `calcular_RSI()`.
- Bandas de Bollinger: inputs de janela e desvios presentes, valores passados para `calcular_bollinger_bands()`.
- Todos os outros indicadores (EWMA, CCI, MACD) funcionam sem alteração.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('pages/10_Fixações.py').read()); print('OK')"` — sem erros de sintaxe.
2. `ls pages/10_Mercado.py` deve retornar erro (arquivo removido).
3. `grep -n "Fixações" pages/10_Fixações.py` deve mostrar hits em `page_title`, `st.title` e no selectbox de indicadores.
4. `grep -n "periodo_k\|periodo_rsi\|janela_bb" pages/10_Fixações.py` deve mostrar definições e usos.
</verification>

<success_criteria>
- Página aparece no nav do Streamlit como "Fixações" (arquivo `10_Fixações.py` lido pelo Streamlit via nome do arquivo).
- `st.title` exibe "Fixações".
- Ao selecionar "Estocástico Lento": sidebar mostra inputs %K (default 14) e %D (default 3).
- Ao selecionar "RSI": sidebar mostra input Período RSI (default 14).
- Ao selecionar "Bandas de Bollinger": sidebar mostra inputs Janela (default 20) e Desvios padrão (default 2.0).
- Cálculo e gráfico refletem os parâmetros inseridos pelo usuário.
- Arquivo `pages/10_Mercado.py` não existe mais.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/18-fixacoes/18-01-SUMMARY.md` seguindo o template em `@C:/Users/netin/.claude/get-shit-done/templates/summary.md`.
</output>
