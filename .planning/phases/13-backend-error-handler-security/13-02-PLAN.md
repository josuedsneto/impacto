---
phase: 13-backend-error-handler-security
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - backend/main.py
autonomous: true
requirements:
  - SEC-03
  - SEC-04

must_haves:
  truths:
    - "POST /api/risco/save with inputs dict exceeding 10KB is rejected with 422 Unprocessable Entity"
    - "GET /api/metas?meta=-1 is rejected with 422 Unprocessable Entity"
    - "GET /api/me with 61 requests in one minute from same IP returns 429 on the 61st request"
    - "PATCH /api/admin/suggestions/{id} with 11 requests in one minute returns 429 on the 11th request"
    - "POST /api/admin/market/backfill/{ticker} with 6 requests in one minute returns 429 on the 6th request"
  artifacts:
    - path: "backend/main.py"
      provides: "RiscoSaveRequest with model_validator for dict size limits; /api/metas with Query bounds; @limiter.limit on all 9 previously-uncovered endpoints"
      contains: "model_validator"
  key_links:
    - from: "RiscoSaveRequest.validate_dict_size"
      to: "json.dumps(self.inputs) > 10_000"
      via: "Pydantic model_validator mode=after"
      pattern: "model_validator"
    - from: "GET /api/metas"
      to: "Query(default=2600, ge=100, le=100_000)"
      via: "fastapi.Query bound on meta param"
      pattern: "Query\\(.*ge=100"
    - from: "@limiter.limit on admin endpoints"
      to: "request: Request parameter"
      via: "SlowAPI requires request param for IP extraction"
      pattern: "@limiter\\.limit.*\\n.*async def.*request: Request"
---

<objective>
Close the two remaining SEC-03 input validation gaps (RiscoSaveRequest untyped dicts and /api/metas unbounded meta param) and backfill @limiter.limit decorators on the 9 API endpoints that currently have no rate limiting, satisfying SEC-04.

Purpose: Eliminates all injection surface gaps and denial-of-service vectors remaining after plan 01. Completes the Phase 13 security hardening.
Output: backend/main.py with validated RiscoSaveRequest, bounded meta query param, and rate limiters on all previously-uncovered non-health endpoints.
</objective>

<execution_context>
@C:/Users/netin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/netin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-error-handler-security/13-RESEARCH.md
@.planning/phases/13-backend-error-handler-security/13-01-SUMMARY.md
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add model_validator to RiscoSaveRequest and bound /api/metas meta param</name>
  <files>backend/main.py</files>
  <action>
    Two targeted changes to backend/main.py:

    CHANGE 1 — RiscoSaveRequest model_validator (SEC-03 gap 1 and 2):

    Add `model_validator` to the Pydantic import at the top of main.py:
      from pydantic import BaseModel, Field, model_validator

    Replace the existing RiscoSaveRequest class (currently at the "Risco history" section):

      class RiscoSaveRequest(BaseModel):
          inputs: dict[str, Any] = Field(default_factory=dict)
          fat_media: float
          custo_media: float
          ebitda_media: float
          results: dict[str, Any] = Field(default_factory=dict)
          label: str | None = Field(default=None, max_length=200)

          @model_validator(mode="after")
          def validate_dict_size(self) -> "RiscoSaveRequest":
              import json
              if len(json.dumps(self.inputs)) > 10_000:
                  raise ValueError("inputs payload too large (max 10KB)")
              if len(json.dumps(self.results)) > 50_000:
                  raise ValueError("results payload too large (max 50KB)")
              return self

    Also add `from typing import Any` to the imports if `Any` is not already imported. Check existing imports first — `typing` imports `Annotated` and `Literal` are present; add `Any` to that import.

    CHANGE 2 — /api/metas meta param bounds (SEC-03 gap 3):

    Add `Query` to the fastapi import line:
      from fastapi import FastAPI, Depends, HTTPException, Request, Query

    Find the `/api/metas` route handler. The research does not show it in the audited file segment but it is referenced in the research as having an unvalidated `meta: float` query param. Search for `def get_metas` or `"/api/metas"` in main.py. If the route exists, update the `meta` parameter:

      meta: float = Query(default=2600, ge=100, le=100_000),

    If the route does not exist in the current main.py (the audit may reference a future endpoint), skip this sub-task and note it in the summary.

    Do NOT change any other route handlers or Pydantic models.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/backend && python -c "
import ast
with open('main.py') as f:
    src = f.read()
tree = ast.parse(src)
# Check model_validator import
assert 'model_validator' in src, 'model_validator not imported'
# Check validate_dict_size method exists
methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'validate_dict_size' in methods, 'validate_dict_size method missing from RiscoSaveRequest'
print('PASS')
" 2>&1</automated>
  </verify>
  <done>RiscoSaveRequest rejects inputs dict exceeding 10KB and results dict exceeding 50KB with 422; /api/metas (if present) rejects meta outside [100, 100_000] with 422; all changes parse cleanly via ast.parse</done>
</task>

<task type="auto">
  <name>Task 2: Add @limiter.limit to 9 endpoints missing rate limiting</name>
  <files>backend/main.py</files>
  <action>
    Add @limiter.limit decorators to each of the 9 endpoints identified in research as missing rate limiting. /api/health is intentionally exempt and must NOT be rate-limited.

    For each endpoint below, the decorator goes IMMEDIATELY above `async def`, AFTER the `@app.METHOD(...)` decorator:

    Endpoints and their limits:

    1. GET /api/me — 60/minute
       @app.get("/api/me")
       @limiter.limit("60/minute")
       async def me(request: Request, user: ...):
       NOTE: This endpoint currently does NOT have `request: Request` in its signature. Add it as the FIRST parameter before `user`.

    2. GET /api/admin/ping — 30/minute
       @app.get("/api/admin/ping")
       @limiter.limit("30/minute")
       async def admin_ping(request: Request, user: ...):
       NOTE: Add `request: Request` as the FIRST parameter.

    3. GET /api/admin/suggestions — 30/minute
       @app.get("/api/admin/suggestions")
       @limiter.limit("30/minute")
       async def admin_list_suggestions(request: Request, user: ..., status: ..., limit: ..., offset: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `user`.

    4. PATCH /api/admin/suggestions/{suggestion_id} — 10/minute
       @app.patch("/api/admin/suggestions/{suggestion_id}")
       @limiter.limit("10/minute")
       async def admin_review_suggestion(request: Request, suggestion_id: ..., body: ..., user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `suggestion_id`.

    5. POST /api/admin/market/backfill/{ticker} — 5/minute
       @app.post("/api/admin/market/backfill/{ticker}")
       @limiter.limit("5/minute")
       async def admin_backfill(request: Request, ticker: ..., user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `ticker`.

    6. POST /api/options/payoff — 30/minute
       @app.post("/api/options/payoff")
       @limiter.limit("30/minute")
       async def options_payoff(request: Request, body: ..., user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `body`.

    7. POST /api/options/bs-price — 60/minute
       @app.post("/api/options/bs-price")
       @limiter.limit("60/minute")
       async def options_bs_price(request: Request, body: ..., user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `body`.

    8. GET /api/admin/config — 30/minute
       @app.get("/api/admin/config")
       @limiter.limit("30/minute")
       async def admin_get_config(request: Request, user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `user`.

    9. PUT /api/admin/config/{key} — 20/minute
       @app.put("/api/admin/config/{key}")
       @limiter.limit("20/minute")
       async def admin_update_config(request: Request, key: ..., body: ..., user: ...):
       NOTE: Add `request: Request` as the FIRST parameter before `key`.

    CRITICAL for admin endpoints: SlowAPI requires `request: Request` in the function signature to extract the client IP for rate limiting. Adding `request` does NOT affect REST semantics — it is not surfaced to callers. The parameter name must be `request` (not `req` or `r`).

    CRITICAL: Do NOT add @limiter.limit to /api/health — it is exempt (public health check used by load balancers).

    After all additions, verify the file parses cleanly with `ast.parse`.
  </action>
  <verify>
    <automated>cd /c/Users/netin/OneDrive/Documentos/Code/impacto/backend && python -c "
import ast, re
with open('main.py') as f:
    src = f.read()
# Must parse cleanly
tree = ast.parse(src)
# Count @limiter.limit occurrences — should be >= 19 (10 existing + 9 new)
count = len(re.findall(r'@limiter\.limit', src))
print(f'limiter.limit count: {count}')
assert count >= 19, f'Expected >= 19 @limiter.limit, got {count}'
# /api/health must NOT have limiter
health_section = src[src.find('async def health'):src.find('async def health')+200]
assert 'limiter' not in health_section, '/api/health should not have rate limiting'
print('PASS')
" 2>&1</automated>
    <manual>Start uvicorn and confirm GET /api/me and GET /api/admin/ping still return correct responses (rate limiting does not break existing behavior for normal request volumes)</manual>
  </verify>
  <done>All 9 previously-unlimited endpoints now have @limiter.limit decorators; all admin endpoints have request: Request as first param; /api/health remains unrated; ast.parse succeeds with no errors; total @limiter.limit count >= 19</done>
</task>

</tasks>

<verification>
Overall phase 13 plan 02 checks:

1. `grep -c "@limiter.limit" backend/main.py` returns >= 19
2. `grep -A5 "async def me" backend/main.py` shows `request: Request` as first param
3. `grep -A5 "async def admin_ping" backend/main.py` shows `request: Request` as first param
4. `grep -A5 "async def admin_list_suggestions" backend/main.py` shows `request: Request` as first param
5. `grep "model_validator" backend/main.py` returns the import and usage
6. `grep "validate_dict_size" backend/main.py` returns the method definition
7. `python -c "import ast; ast.parse(open('backend/main.py').read()); print('OK')"` from project root
8. No @limiter.limit on /api/health
</verification>

<success_criteria>
- POST /api/risco/save with oversized inputs dict rejected with 422 (not 500)
- All 9 endpoints have explicit rate limits; no unlimitable endpoint exists except /api/health
- admin endpoints accept request: Request without breaking existing functionality
- backend/main.py parses cleanly (no syntax errors from the additions)
- Phase 13 complete: REL-01, REL-02, SEC-01 (verified compliant), SEC-02, SEC-03, SEC-04 all addressed
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-error-handler-security/13-02-SUMMARY.md`
</output>
